# PR #281 — configure.py presents Intelligence as recommended and bundled addons as facts

One round. `invariants.md` is the list it was briefed with, including the
revisions approved before review and after round 1. Round 1 found no behaviour
defect; its fixes were tests, a deleted comment and the wording of two printed
lines, so no second round was run.

Read the code for what the system does now. These files quote it as it stood at
the SHA the round reviewed.
