# PR #299 review, round 2

Reviewed: fix commit `9b6c582f5fb5f05d61f629a849a99e6558eab9dd` on its own (parent `a0f45198`), worktree `/Users/libre/Sources/video_share-ki`.
Baseline: PropertiesPanel, EmptyState, GlobalSearch, i18n-keys tests pass (4 files / 148 tests).
Submodules at the pinned SHAs (`git submodule status` shows no `+`): knowledge `0c9d0b6a`, intelligence `dbdcbcf1`, media_import `d8c8d9d4`, cloud-sync `812ea86e`.

## Rebase check

`a0f45198^` is `f1ccb735` = `origin/develop`. `git diff origin/develop a0f45198 -- SidebarLibrarySection.tsx DriveHome.tsx` shows only the lucide import line and the one icon element in each file (sidebar Recently Viewed `Clock` -> `History`; Home Recently Added `Clock` -> `FilePlus`). `git diff --stat origin/develop a0f45198` is the same 12 files / +132 -26 as the round-1 commit `ac625d2b`. #298's changes are intact.

## Origin table vs. actual writers

Grepped `origin` across `backend/`, `frontend/src`, `mcp-server/`, and every `addons/*` (app + frontend, excluding tests):

| origin | writer(s) |
|---|---|
| `webclip` | `addons/knowledge/app/main.py:73`, `addons/knowledge/app/routers/clips.py:74,87` |
| `detailed_summary` | `addons/knowledge/app/schemas.py:49` (distill default) + the only distill caller `addons/intelligence/frontend/KnowledgeSaveDialog.tsx:142` |
| `source_capture` | `addons/knowledge/app/routers/captures.py:162,197`, `addons/knowledge/app/services/capture_markdown.py:71` |
| `ask_answer` | `addons/intelligence/frontend/askNoteFormat.ts:99`, `addons/knowledge/app/schemas.py:70` (NoteCreate Literal default) |

No origin writer in `backend/`, `mcp-server/`, `media_import`, `cloud-sync`; `note_from_file.py:43` writes no origin. `manual` has a message key but no production writer (only knowledge tests). The test's declared table (`webclip`, `detailed_summary`, `source_capture`, `ask_answer`) equals the writer set exactly. `distill` accepts any `str` for `origin`, but its only caller sends `detailed_summary`.

## Mutation table

P-rows: edit `messages-core/*`, re-run `scripts/merge-addon-messages.mjs` (the test mock reads the generated `src/messages/en.json`), run PropertiesPanel + i18n-keys, restore and re-merge (generated files `cmp`-identical to backup afterwards).

| # | File | Mutation | want | result | killed by |
|---|---|---|---|---|---|
| P1 | `messages-core/en.json` | delete `ask_answer` | kill | KILLED | PropertiesPanel "ask_answer in English"; i18n-keys (3) |
| P2 | `messages-core/ja.json` | delete `ask_answer` | kill | KILLED | PropertiesPanel "ask_answer in Japanese"; i18n-keys (3) |
| P3 | `messages-core/ja.json` | `ask_answer` value -> `""` | kill | KILLED | "ask_answer in Japanese"; i18n-keys "no empty string values in ja.json" |
| P4 | `messages-core/en.json` | `ask_answer` value -> `"Web clip"` | kill | KILLED | "ask_answer in English" |
| P6 | both | delete `manual` | live | LIVE | expected: no writer emits `manual`; not in the declared table |
| P7 | `messages-core/en.json` | move `ask_answer` out of `propertiesPanel.origin` to `propertiesPanel` | kill | KILLED | "ask_answer in English"; i18n-keys (3) |
| E1 | `EmptyState.tsx` | `no-recent` icon -> `Clock` | kill (inv 5) | KILLED | "gives each variant the icon the table names" |
| E2 | same | `no-recent-profile` icon -> `Clock` | kill (inv 5) | KILLED | same |
| E3 | same | `no-recent-added` icon -> `History` | kill (inv 5) | KILLED | same |
| E4 | same | `no-recent` icon -> `FilePlus` | kill (inv 5) | KILLED | same |
| G8 | `GlobalSearch.tsx` | delete `addToHistory` in `handleSelect` | kill (inv 4) | KILLED | "records the query behind a chosen result under the drive it was made on, and nowhere else" |
| G9 | same | delete `addToHistory` in `navigateToSearchPage` | kill (inv 4) | KILLED | "records a submitted term under the drive it was made on, and nowhere else" |
| G10 | same | `navigateToSearchPage` persists to `"zz"` | kill (inv 4) | KILLED | "records a submitted term..." |
| G11 | same | `handleSelect` persists to `"zz"` | kill (inv 4) | KILLED | "records the query behind a chosen result..." |
| G12 | same | `handleSelect` also persists to `"other"` | kill (inv 3/4) | KILLED | "records the query behind a chosen result..." |
| G13 | same | `navigateToSearchPage` also persists to `"other"` | kill (inv 3/4) | KILLED | "records a submitted term..." |
| G14 | same | `handleRemoveHistory` removes from `"other"` instead | kill (inv 7) | KILLED | "removes a recent term from the current drive only"; "stays on its row when a row above it is removed" |
| G15 | same | `handleRemoveHistory` removes from `"other"` and the current drive | kill (inv 7) | KILLED | "removes a recent term from the current drive only" |
| G16 | same | `handleRemoveHistory` updates state without persisting | kill (inv 7) | KILLED | "removes a recent term from the current drive only" |
| G17 | same | `handleSelect` passes `query.trim()` | live | LIVE | expected: `addToHistory` trims already |

Round-1 survivors G8-G11 (F3) are now killed. Tree restored after every mutation; `git status --short` clean at the end. Full frontend suite at the SHA: 495 files / 7167 passed, 2 todo; `tsc --noEmit` clean; eslint on the four touched source/test files clean.

## Findings

### F1 [pre-existing] OriginRenderer's try/catch does not fall back, because next-intl returns the key path for an unknown origin (already filed).

No `[introduced]` findings.

## Trajectory

Round one (`a0f45198`) added state and branches in implementation: a drive-change effect in `useInlineRename`, and a drive-tagged `historyData` with a `drive === drive` match in `GlobalSearch`. This fix (`9b6c582f`) adds no branch, state or prediction to implementation code: its implementation diff is two icon constants in `EmptyState.tsx` and one translation key per locale; everything else is tests. It does not repeat round one's shape. The one extra thing the fix declares is the origin table in the test, which is a declared expected set (Detector rule 5), not a runtime prediction.

## Checked, no finding

- **Detector rules for the origin table.** Expected set is declared, not derived from `messages` (rule 5); it is iterated with `it.each`, no `>=` (rule 1). It cannot follow a new writer in an addon repo automatically; that is the update-with-the-change case rule 1 names.
- **The Japanese `it.each` overlaps i18n-keys.** P2/P3 are killed by both it and i18n-keys; redundant but not wrong.
- **`manual` absent from the table.** No production writer (see table above); P6 live as declared.
- **Other surfaces naming Recently Viewed / Recently Added.** Grepped `recentlyViewed`, `recentAdded`, `view=recent`, `no-recent` across `frontend/src` and `addons/*/frontend`: sidebar, Home (Recently Viewed row; Continue Watching has no icon prop), `FolderContent` empty states. `FolderBrowser` / page header render no icon for these views. Knowledge's `NotesLanding` "recent" is a different concept. Inv 5 surfaces are all pinned by literal-icon tests.
- **`Clock` elsewhere.** Removed from `EmptyState.tsx` imports only; `SearchEmptyState` recent-search-terms `Clock` is a separate concept (as in round 1).
- **Test `[0]` index on "Remove ... from history".** Two render sites exist; G14 shows the click reaches the handler.
- **Importing `@/messages-core/ja.json` in a component test.** Resolves under vitest and `tsc`; reads the edit-location file, not the generated one, so it does not need a merge step.
- **Generated-messages dependency of the English test.** Same as round 1: local runs need `merge-addon-messages.mjs` after editing `messages-core/en.json`; CI merges first.

TOTAL: 1 findings
