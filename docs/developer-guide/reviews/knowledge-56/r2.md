# Review R2 — knowledge c2d07f1 (fix on 9104fcf), feat/graph-focus-from-note

Reviewed: `git show c2d07f1` against parent `9104fcf`, in `/Users/libre/Sources/video_share/addons/knowledge`.
Tests run from `/Users/libre/Sources/video_share/frontend` (per-file symlinks into the addon).
Baseline at c2d07f1: 3 files, 40/40 pass (37 at 9104fcf + Escape test + 2 page `it.each` cases).
Each mutation applied by a script that restores the file in a `finally`; `git status --short` empty at the end, HEAD still c2d07f1.

## Mutation table

G = `frontend/ConnectionsGraph.tsx`, P = `frontend/pages/connections.tsx`

| id | mutation | want | result | killed by |
|---|---|---|---|---|
| N1 | P `get("focus") \|\| null` -> `?? null` (R1's code) | kill | killed | page "shows the whole graph without a notice for an empty focus query" |
| N2 | P `\|\| null` -> `?? ""` (R1's M15) | kill | killed | both page "without a notice" cases |
| N3 | G Escape handler: drop the focus branch (only clears selection) (R1's M19) | kill | killed | "returns to the whole graph on Escape" |
| N4 | G Escape handler body emptied (Escape does nothing) | kill | killed | "returns to the whole graph on Escape", zoom-keys test |
| N5 | G Escape shortcut `enabled` -> `selectedId !== null` only | live | live | — (initial focus also selects the file, so the shortcut is enabled either way; equivalent on every reachable arrival path) |
| N6 | G Escape shortcut `enabled` -> `false` | kill | killed | "returns to the whole graph on Escape", zoom-keys test |
| N7 | G Escape out of focus also clears the selection | live | live | — (invariant 6 says nothing about the selection) |
| N8 | G initial-focus effect deps add `focusedId` (re-applies after reset) | kill | killed | "returns ... when the focus is reset", "returns ... on Escape" |
| N9 | G `initialFocusMissing` drop `initialFocusId !== null` | kill | killed | component "stays unfocused when no file is given", both page "without a notice" cases |
| N10 | G effect guard `!initialFocusId` -> `initialFocusId === null` | live | live | — (the page no longer passes `""`; equivalent on the page) |
| N11 | G notice guard `initialFocusId !== null` -> `!!initialFocusId` | live | live | — (same: `""` cannot reach the component from the page) |
| N12 | G notice key `focus.notInGraph` -> `focus.notInGraphX` | kill | killed | "says the file is not in the graph...", "says so even when the drive has only unlinked notes" |

Escape test "passing for the wrong reason": N4 (Escape does nothing) and N6 (shortcut never registered) both fail it, and N3 (Escape only clears the selection) fails it, so the 5-node / no-banner assertion is reached only through the focus branch of the handler. Nothing else in the test resets the focus (no click, no rerender). N8 shows it also holds "does not re-apply the initial focus" for the Escape path.

Page "without a notice" tests are not vacuous: N1, N2 and N9 each turn the notice on at page level and are killed there; the asserted string `knowledge.connections.focus.notInGraph` matches the component's namespace (`useTranslations("knowledge.connections")`). Under N12 those absence assertions become vacuous, but the component presence tests pin the key, so the pair still holds.

## Invariant check

- 1: untouched by c2d07f1 (ConnectionsLink not in diff).
- 2, 3 (revised wording): code untouched; en/ja `notInGraph` now state the file is not in the graph, not that it has no connections. Held by R1 tests (N12 kill).
- 4: untouched.
- 5 (revised, covers `?focus=`): now held at page level for both absent and empty values (N1, N2, N9 killed).
- 6: reset button and Escape both held (N3, N4, N6, N8 killed).
- 7: no request code in the diff; unchanged from R1 probe.
- 8 (new): no code in the diff touches it; the id still only meets the drive-scoped response. No test holds it, same as R1 found; no mutation of this diff can break it.

`|| null` also maps nothing else to null that should be a focus: `get()` returns `string | null`, and the only falsy string is `""`.

## Question 1 — did c2d07f1 do what the triage says, and what did it break

- F1: fixed. The page-level "no focus" and "empty focus" cases now fail on the R1 survivor (N2) and on the pre-fix code (N1).
- F2: fixed. The Escape-from-initial-focus path is tested and the R1 survivor (N3) is now killed.
- F4: fixed via F1 — `|| null` normalises `?focus=` to null, so the effect and the notice agree on "no focus" when rendered from the page. The component still treats `""` inconsistently (`!initialFocusId` vs `!== null`), but only the page renders it with a query value (N10, N11 live and equivalent).
- F5: fixed by rewording both locales.
- Broken: nothing found. No test weakened or removed; the renamed test keeps the same assertions.

## Question 2 — trajectory

No. c2d07f1 adds no branch, no state and no prediction on top of 9104fcf. The only code change replaces `??` with `||`, which removes a distinction (`""` vs null) rather than adding one; the rest is two tests and two strings.

## Findings

None.

F3 [pre-existing from R1's point of view, closed in triage]: unchanged by c2d07f1; `|| null` does not touch the prop-change path.
F6 [pre-existing from R1's point of view, closed in triage]: unchanged by c2d07f1; the notice condition is not in the diff.

TOTAL: 0 findings
