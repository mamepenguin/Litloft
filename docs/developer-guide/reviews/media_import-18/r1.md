# media_import #18 レビュー round 1

対象: Litloft-media-import-addon `e18f07aa7292d83e09e388790dff8ee404429031`（parent `09a4407`）。
core は `ee9717ce2ac8dfccef19ac28544784dad2e10cd0` の tarball。media_import は実ディレクトリとして `addons/media_import` に展開（scratch 内、worktree 不使用）。
`[introduced]` の比較には parent tarball を同じ core に載せた別ツリー／別イメージ（`mi-rev18-parent`）を使った。

## ベースライン（HEAD）

- frontend: `pnpm exec vitest run src/addons/media_import` → 185 passed（初回は docker のフルスイートと並走させた負荷下で 1 failed。単独再実行で 185 passed。どのテストかは記録できていないので、負荷依存のタイムアウトとして扱い finding にはしない）。
- `pnpm exec tsc --noEmit` → exit 0。
- backend（CI と同じ `PYTEST_ADDOPTS`）: 291 passed、`--cov-fail-under=80.28` exit 0。
- `test_watch_surface.py` 単体: 26 passed。

## COV_FLOOR の実測（invariant 8）

| 対象 | lines | branches | percent_covered | files | floor | exit |
|---|---|---|---|---|---|---|
| HEAD | 1564/1901 | 301/422 | 80.28411536805855 | 14 | 80.28 | 0 |
| HEAD | 同上 | 同上 | 同上 | 14 | 80.29 | 1（`FAIL Required test coverage of 80.29% not reached`） |
| parent | 1577/1913 | 306/426 | 80.5044890979051 | 14 | 80.50 | 0 |

- `scripts/check-coverage-population.py cov.json 80.28` → exit 0（`coverage 80.2841% against a floor of 80.28%, over 14 production modules matching an independent walk`）。
- 作者申告値（before 80.5045% / after 80.2841% / 14 modules / 80.28 exit 0・80.29 exit 1）はすべて再現した。80.28 は 80.2841 の小数 2 桁切り捨てで一致。

## Findings

### F1 [introduced] `in_progress` バッジ状態をどのバックエンドテストも保持していない

- invariant: 3（progress badges unchanged）、9（viewer 自身の進捗がバッジに載る）
- ミューテーション（`backend/router.py` `_playback_state`）:
  ```diff
  -    return "in_progress"
  +    return "not_started"
  ```
  - HEAD: `want=kill` → **26 passed（survive）**
  - parent 09a4407 に同じ変更: 1 failed — `TestContinueWatching::test_includes_library_only_and_one_off_imports`（`all(i["playback"]["state"] == "in_progress" ...)`）
- 削除された Continue レーンのテストが、`in_progress` を返す唯一の検査だった。残った `TestPlaybackBadges` / `TestRegularSourceCap` は `completed` と `None` しか assert していない（`grep in_progress test_watch_surface.py` は 0 件）。
- ユーザーから見える影響: `WatchCard.tsx` は `playback.state === "in_progress"` のときだけ進捗バーを描く。この分岐が壊れると、途中まで見た動画の進捗バーが regular/feed の両レーンから消えるが、CI は green のまま。frontend の「shows how far into a started video」テストは `state: "in_progress"` を mock で与えているので、サーバー側の判定は保持しない。
- 不足しているテスト: viewer 自身の `30/300` 行を持つ feed（または regular）アイテムで、`playback == {position: 30, duration: 300, state: "in_progress"}` を assert するもの。position と duration を入れ替えても生き残る点（B12、pre-existing）も同じ assert で潰れる。

### F2 [introduced] フロントのレーン limit を値として保持するテストがなくなった

- invariant: 2（regular 12 / feed 12）
- ミューテーション（`frontend/api.ts`）:
  ```diff
  -  feed: { limit: 12, pageable: true },
  +  feed: { limit: 5, pageable: true },
  ```
  - HEAD: `want=kill` → **20 passed（survive）**
  - parent に同じ変更: 1 failed — `asks each lane for its own size`（削除された `expect(WATCH_LANE_CONFIG.continue.limit).toBeLessThan(WATCH_LANE_CONFIG.feed.limit)` の 6 < 5 が偽になる）
- 値を実際に確かめていたのはこの assertion だけだった。残った `asks each lane for its own size` と `pages a lane without asking for a total` は、期待値を `WATCH_LANE_CONFIG[lane].limit` から組み立てている（review-workflow の detector rule 5「観測から期待値を組み立てない」）。そのため limit をいくつに変えても両辺が一緒に動く。
- 付随する survivor（parent でも生き残るので pre-existing。ただし根は同じ）: `regular.limit 12→6`（M7）、`feed.limit 12→40`（M8）。
- ユーザーから見える影響: Watch の各レーンの件数と「Show more」のページサイズが黙って変わる。feed を 5 にすると 5 件ごとのページングになる。
- 不足しているテスト: `listWatch` の呼び出し引数を定数で宣言し（regular と feed の両方に `{ limit: 12 }`）assert するもの。

### F3 [pre-existing] Watch のセクション順（Regular → Recent）を保持するテストがない

- invariant: 2（Regular sources, then Recent）
- ミューテーション（`frontend/watch/WatchViewPresenter.tsx`）: `watch-lane-regular` と `watch-lane-feed` の 2 つの `<WatchLaneSection>` を入れ替える。
  - HEAD: `want=kill` → **20 passed（survive）**
  - parent で同じ入れ替え（continue は先頭のまま、regular と feed を交換）: 17 passed（survive）→ pre-existing
- この PR で先頭のセクションが Continue から Regular に変わり、`displayMode.regular.hint`（「shown prominently at the top of Watch」）が初めて文字どおりの意味を持つようになった。それでも順序は未検査のまま。
- ユーザーから見える影響: Regular sources が Recent の下に回っても CI は通る。
- 不足しているテスト: `watch-view` 直下の `data-testid^="watch-lane-"` を文書順に並べ、`["watch-lane-regular", "watch-lane-feed"]` と等しいことを assert するもの。

### F4 [pre-existing] Watch/Manage の着地判定が `regular` だけの購読を検査していない

- invariant: 5
- ミューテーション（`frontend/Page.tsx`）:
  ```diff
  -        const surfaced = rows.some((r) => r.display_mode !== "library");
  +        const surfaced = rows.some((r) => r.display_mode === "feed");
  ```
  - HEAD: `want=kill` → **Page.test.tsx 7 passed（survive）**
- `Page.tsx` / `Page.test.tsx` はこの PR の diff に含まれない（parent と同一）ので pre-existing。`Page.test.tsx` の fixture は `library` と `feed` だけ。
- ユーザーから見える影響: `regular` の購読しかないドライブが Manage に着地し、Watch の空状態の文言も `none` 側になる。
- 不足しているテスト: `makeSubscription("regular")` だけの状態で Watch に着地することを assert するもの。

### F5 [pre-existing] バッジの `position <= 0` ガードと 90% しきい値が未検査

- invariant: 3（progress badges unchanged）
- ミューテーション（`backend/router.py` `_playback_state`）:
  - B6: `if duration <= 0 or position <= 0:` → `if duration <= 0:` — HEAD 26 passed、parent 31 passed（両方 survive）
  - B7: `position >= duration * 0.9` → `* 0.95` — HEAD 26 passed、parent 31 passed（両方 survive）
- 削除された `test_excludes_completed_and_view_only` が 0/0 行を扱っていたのは Continue の SQL 述語であって、`_playback_state` ではない。したがって削除によるカバレッジ低下ではなく pre-existing。
- ユーザーから見える影響: B6 では `0/300` の行（プレイヤーが duration を報告したが位置 0）が in_progress の 0% バーになる。B7 では 90〜95% 視聴済みの動画が Home の Continue Watching の判定（90%）と食い違い、「Watched」にならない。
- 不足しているテスト: `0/300` → `not_started`、`270/300` → `completed`、`269/300` → `in_progress` の境界 3 点。

### F6 [introduced] README の表が、削除したレーンを現存するものとして書いている

- invariant: none（散文。ただし読者が誤ったコード変更をしうるので報告する）
- `README.md` の「Watch 面」表、`library` 行の「Watch での扱い」列: `レーンに出さない。再生途中なら「再生途中」には出る`
- parent では正しかった記述が、この PR で偽になった（同じ README の API 表の `lane=` はこの PR で更新済みで、この行だけ取り残されている）。これを読んだ人は、library 購読の再生途中動画を Watch に出す経路が存在すると考え、欠けたレーンを「バグ」として戻そうとしうる。
- 対処: 「再生途中なら「再生途中」には出る」の一文を削除する。

### F7 [introduced] テストファイルの残骸（`last_played_at` 引数、空行）

- invariant: none
- `_seed_history(..., last_played_at=...)` を渡す呼び出し元がなくなった（freshness テストと一緒にすべて削除された）。引数と `or` 分岐は死んでいる。
- 追加された `test_another_viewers_progress_is_not_a_badge` の `def` が、直前のテストの `assert items[0]["playback"] is None` の行に空行なしで続いている。挙動には影響しないし、lint も CI にない。
- ユーザーから見える影響: なし。記録のみ（B 相当）。

## Mutation table

| id | diff summary | want | result | verdict |
|---|---|---|---|---|
| M1 | Presenter `anyLoading = regular.loading`（feed.loading を除去） | kill | 1 failed（feed lane still loading） | killed |
| M2 | `anyLoading = feed.loading`（regular.loading を除去） | kill | 1 failed（regular lane still loading） | killed |
| M3 | `nothingToShow` から `regular.items.length === 0` を除去 | kill | 1 failed（regular lane has videos） | killed |
| M4 | `nothingToShow` から `feed.items.length === 0` を除去 | kill | 1 failed（feed lane has videos） | killed |
| M5 | `nothingToShow` から `!anyLoading &&` を除去 | kill | 2 failed | killed（parent で同等の `anyLoading = false` は survive。この PR が追加したテストで新たに保持された） |
| M6 | 空状態の文言 surfaced/none を反転 | kill | 2 failed | killed |
| M7 | `regular.limit 12→6` | kill | 20 passed | survive → F2（pre-existing 部分） |
| M8 | `feed.limit 12→40` | kill | 20 passed | survive → F2（pre-existing 部分） |
| M9 | `regular.pageable false→true` | kill | 1 failed | killed |
| M10 | `feed.pageable true→false` | kill | 1 failed | killed |
| M11 | regular と feed のセクション順を入れ替え | kill | 20 passed（parent でも survive） | survive → F3 |
| M12 | Container に `useWatchLane(drive, "regular")` を追加（3 リクエスト目） | kill | 4 failed | killed（invariant 1） |
| M13 | `feed.limit 12→5` | kill | HEAD 20 passed / parent 1 failed | survive → F2（introduced） |
| M14 | Page `!== "library"` → `=== "feed"` | kill | 7 passed | survive → F4 |
| M15 | en.json に `lane.continue` を戻す（merge 後にスイート全体） | live | 185 passed | live（ユーザーから見えない。invariant 7 は grep で確認: addon 内と merge 後の messages に `continue` キーなし、参照もなし） |
| M16 | TS `WatchLane` に `"continue"` を戻す | kill | tsc exit 2（`TS2741 Property 'continue' is missing ... Record<WatchLane, WatchLaneConfig>`） | killed（tsc で。tsc は CI で走る） |
| B1 | backend `WatchLane = Literal["continue","regular","feed"]` | kill | 1 failed（`test_unknown_lane_is_rejected[continue]`） | killed |
| B2 | `load_playback_markers` の `viewer_id = :viewer` を `1=1` に | kill | 1 failed（`test_another_viewers_progress_is_not_a_badge`） | killed |
| B3 | `if viewer_id is not None and rows` → `if rows` | live | 26 passed | inert: `WatchHistory.viewer_id` は primary key で NULL 行は作れず、`viewer_id = NULL` はどの fixture でも 0 行 |
| B4 | markers 読み取りの `except Exception` → `except ZeroDivisionError` | kill | 1 failed（`test_progress_failure_does_not_hide_videos`） | killed |
| B5 | `per_source_limit` を lane に関係なく `REGULAR_PER_SOURCE` に | kill | 2 failed | killed |
| B6 | `_playback_state` の `or position <= 0` を除去 | kill | HEAD 26 passed / parent 31 passed | survive → F5 |
| B7 | 完了しきい値 `0.9 → 0.95` | kill | HEAD 26 passed / parent 31 passed | survive → F5 |
| B8 | `list_watch_lane` から `f.deleted_at IS NULL` を除去 | kill | 1 failed（`test_missing_and_trashed_files_are_excluded`） | killed |
| B9 | `list_watch_lane` の `f.drive = :drive` を `1=1` に | kill | 26 passed | survive。同じ fixture は `s.drive = :drive` でも落ちるので、差を観測するには別ドライブのファイルを d の購読にリンクする必要がある（通常の取り込みでは起きない）。pre-existing（`test_other_drives_never_appear` の feed 側 assert は parent と同一）。finding にはしない |
| B10 | markers 読み取りを `lane == "feed"` のときだけに | kill | 1 failed（`test_completed_videos_are_not_treated_differently`） | killed（regular のバッジも保持されている） |
| B11 | `_playback_state` の最後の `return "in_progress"` → `"not_started"` | kill | HEAD 26 passed / parent 1 failed | survive → F1（introduced） |
| B12 | `WatchPlayback` の position/duration を入れ替え | kill | HEAD 26 passed / parent 31 passed | survive（pre-existing）。F1 の不足テストで同時に潰れる |

すべてのミューテーションは適用直後に diff を表示し、実行後にツリーを復元した。frontend は `diff -rq` で HEAD のバックアップと一致することを確認し、messages を再 merge した。backend は scratch 上のコピーを bind mount したので、ソースツリーには触れていない。

## 削除で失われたカバレッジの確認

- viewer 分離: `test_another_viewers_progress_is_not_a_badge` が追加され、B2 で killed。保持されている。
- viewer なし: `test_lanes_render_without_a_viewer[regular|feed]` が追加された。alice の履歴を seed したうえで anon で呼ぶので、viewer を固定値にするような変更も検出できる。B3 は inert。
- trashed/missing（履歴あり）: feed 側の assert が残り、B8 で killed。`list_watch_lane` は regular と feed で共通の SQL。
- 他ドライブ: 削除された `continue` の assert は `list_watch_continue` 固有の SQL を検査していたので、SQL ごと消えた。残った feed 側の状態は B9 のとおり（pre-existing）。
- view-only 行とバッジ: 削除されたテストが検査していたのは Continue の SQL 述語で、バッジ関数ではなかった。F5（pre-existing）。
- `in_progress` の状態: **失われた** → F1。
- レーン limit の値: **失われた** → F2。

TOTAL: 7 findings
