# Review — Phase 2 intelligence: `folder-ai-actions` moved to `folder-bulk-actions-menu`, commit 9a27303

Repo: `addons/intelligence` (worktree `/Users/libre/Sources/video_share-bulk-menu`), parent `095679a` (origin/main).
Core worktree at `7d58efff` (`feat/folder-bulk-actions-menu`), with the submodule checked out at 9a27303.

## Baseline

- `npx vitest run src/addons/intelligence/FolderAIActionsMenuItems.test.tsx`: 29 passed.
- `npx vitest run` (whole core suite, addon linked by `setup-addons.sh`): 491 files, 7110 passed, 2 todo.

## What this commit establishes, and what it does not

Establishes:
- `manifest.json` lists `folder-ai-actions` under exactly one slot, `folder-bulk-actions-menu`,
  and no other slot holds that id (M1–M5 all killed by the new test). Core's registry
  (`backend/app/services/addon_registry.py`) reads this file directly, so this is the
  declaration core will act on.
- The component still returns `null` on an empty `fileIds` (M8 killed) — the part of
  invariant 4 that lives in the addon.
- The component still calls `onRequestClose` from each row when the confirm is accepted
  (M10 killed), and the confirm/queue/toast flow is unchanged (the handlers differ from
  `095679a` only by deleted comments) — the addon's share of invariant 5.
- No core test at `7d58efff` depended on intelligence declaring `folder-actions-menu`:
  the whole suite is green with the move, and reverting the slot (M1full) is caught only
  by the addon's own new test.

Does not establish:
- Invariants 1 and 2 end to end. Nothing here renders the real `AddonSlot` with the real
  manifest and the real `slots.ts`; that is Phase 3's integration test. See F2: the
  registry key can drift from the manifest id with every test green.
- Focus landing on the `…` trigger (invariant 5) and the separator behaviour (invariant 4)
  are core-side and were reviewed in #296.
- Ordering: if this lands on intelligence `main` before #296 ships, a core without the
  `folder-bulk-actions-menu` host drops the AI rows entirely (plan's HIGH risk). Nothing
  in this commit guards that; it is a merge-order matter.

## Findings

### F1 [pre-existing] Low — declining the confirm is not held to close the menu
`frontend/FolderAIActionsMenuItems.tsx:88-89` (same shape in `:112-113`, `:138-139`);
test `frontend/FolderAIActionsMenuItems.test.tsx:73-84`.

M9: move `onRequestClose?.()` below `if (!confirmBatch(...)) return;` in `handleTags` —
want=kill, **survived** (29/29). The only close test runs with `confirm` answering `true`,
so with M9 a reader who presses *Create AI tag candidates…* and cancels the question is
left with the `…` menu still open, which invariant 5 ("pressing a bulk row closes the `…`
menu") rules out. The handler and this test are unchanged from `095679a` apart from
deleted comments and the `describe` title, so the gap predates the move; the move only
changes which menu is left open.

### F2 [pre-existing] Low — `slots.ts`'s key is not tied to the manifest id
`frontend/slots.ts:23`; `manifest.json:145`.

M6: rename the `slots.ts` key `"folder-ai-actions"` → `"folder-ai-actionz"` — want=kill,
**survived** in the target file and in the whole suite (M6full: 491 files, 7110 passed).
The new placement test reads the manifest only, and every behaviour test imports the
component directly, so a registry key that no longer matches the manifest id passes
everything. In the app, core would list the entry in `…` (the `hasSlot` check sees the
declaration, so the separator shows) and `AddonSlot` would find no component: a separator
with no rows under it on every folder, and no AI rows anywhere — invariants 1 and 4.
Intelligence has no manifest↔`slots.ts` parity test at `095679a` either (knowledge has
one, `addons/knowledge/frontend/__tests__/manifest.test.ts`). Phase 3's integration test
closes this only if it renders through the real `slots.ts` rather than a stubbed registry.

## Mutation table

All run from core `frontend/` against `src/addons/intelligence/FolderAIActionsMenuItems.test.tsx`
unless marked "full"; tree restored after each (`git checkout -- <file>`).

| # | Mutation | want | result | killed by |
|---|---|---|---|---|
| M1 | manifest slot `folder-bulk-actions-menu` → `folder-actions-menu` (revert the move) | kill | killed | `is declared in the folder toolbar's … menu, not in Add` |
| M1full | M1, whole suite | kill | killed (1 test, same one; no core test notices) | same |
| M2 | slot → `folder-bulk-actions-menux` | kill | killed | same |
| M3 | entry id → `folder-ai-actionz` | kill | killed | same |
| M4 | keep bulk entry, also declare `folder-ai-actions` in `folder-actions-menu` | kill | killed | same |
| M5 | `file-actions-menu`'s `index-details` id → `folder-ai-actions` (second holder elsewhere) | kill | killed | same |
| M6 | `slots.ts` key → `folder-ai-actionz` | kill | **survived** | — (F2) |
| M6full | M6, whole suite | kill | **survived** | — (F2) |
| M7 | new test: drop `realpathSync`, resolve lexically from `import.meta.url` | kill | killed (ENOENT: `import.meta.url` is the link path under `frontend/src/addons/intelligence/`) | placement test |
| M8 | delete `if (fileIds.length === 0) return null;` | kill | killed | `renders nothing when the folder has no files` |
| M9 | `onRequestClose` after the confirm in `handleTags` | kill | **survived** | — (F1) |
| M10 | delete `onRequestClose` from `handleVision` | kill | killed | `asks the host to close its menu from visionFolderButton` |
| M11 | `handleTags` top guard: drop `inFlight.has(...)` | live | survived | — (the claim in `claim()` still refuses; only a needless confirm is shown, from a second live copy, which the `…` menu cannot have open at once) |
| M12 | `claim()`: drop `inFlight.has(key)` | live | survived | — (the handler's top guard refuses first) |
| M15 | M11 + M12 together | kill | killed (5+) | `refuses a second claim even from a copy that has not re-rendered`, … |
| M13 | `claim()`: drop `inFlight.add(key)` | kill | killed | `queues a batch once…`, `refuses a second claim…` |
| M14 | tags row `disabled={false}` | kill | killed (10; cascades through the module-level `inFlight` set left held by the aborted test) | `queues a batch once…` |

### Path resolution of the new test

- Dev link tree (`setup-addons.sh`, per-file symlinks) — the layout CI uses too
  (`.github/workflows/ci.yml` runs `./setup-addons.sh` before every vitest job): works, and
  `realpathSync` is load-bearing there (M7).
- Copied layout (`frontend/Dockerfile`: `cp -r "$addon_dir/frontend/"* src/addons/$name/`):
  simulated by copying the test and `messages/` into `frontend/src/addons/zz_copycheck/`
  (removed afterwards). It fails with
  `ENOENT … frontend/src/addons/manifest.json`, because that layout copies `frontend/`
  only and `manifest.json` is not there at any path. No vitest run uses that layout (the
  Dockerfile runs no tests), and knowledge's `manifest.test.ts` has the same dependency, so
  this is not a finding. If a test stage is ever added to that image, both addons' manifest
  tests go red together.

## Phase 3 checklist (not findings)

- [ ] Bump `addons/intelligence` to the merged commit; `git diff --submodule=short addons/`.
- [ ] Integration test through the real `AddonSlot` **and the real `slots.ts`** (closes F2):
      three AI rows in `…`, none in Add; knowledge / media_import rows in Add, none in `…`.
- [ ] `docs/addons/intelligence.md:762` — "inside the folder toolbar's **Add** menu".
- [ ] `docs/ADDON-DEVELOPMENT.md:1559` — intelligence slot table row still lists
      `folder-actions-menu` / "rows of the folder toolbar's **Add** menu".
- [ ] `docs/user-guide/file-browsing.md` — `…` menu description gains the addon rows
      (line 59 already says the batch AI actions are not Add rows on Home).
- [ ] Browser check of the `…` menu at 375px and 768–1119px, including declining the confirm (F1).
- Checked and not needing a change: `docs/ADDON-DEVELOPMENT.md:850-851` (generic slot
  descriptions), `docs/addons/overview.md:100`, core `FolderToolbar.test.tsx:72-78` and
  `DriveHome.test.tsx` (mocked slots, green with the move), knowledge / media_import tests
  (their own `folder-actions-menu` entries).

## Tree state

`git -C addons/intelligence status --porcelain`: empty. Core `git status --porcelain`:
` M addons/intelligence` only (pre-existing pointer).

TOTAL: 2 findings
