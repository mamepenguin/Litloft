# intelligence #71 — the folder AI actions move to the … menu

Reviews of a pull request in the intelligence addon repository, kept here because that
repository has no review record of its own. One round, reviewing `9a27303`, with the
invariants it was briefed with. F2 is closed by `FolderToolbar.bundledRows.test.tsx`
in core.

Read the code for what the system does now. These files quote it as it stood at
the SHA the round reviewed.
