# PR #270 — a file menu row's dialog stays open

One round. `invariants.md` is the list it was briefed with, including the item
the supervisor added at approval. The fixes after round 1 were tests only (F2,
F3), so there was no second round. F1 is knowledge's Create note dialog
rendering inside the menu; it is fixed in the knowledge repository.

Read the code for what the system does now. These files quote it as it stood at
the SHA the round reviewed.
