# PR #267 review, round 2

Reviewed SHA: `49fd717f1488fd434631593c543cf15e4bedbeeb` (detached worktree). Submodules at intelligence `c4fe4a41`, knowledge `95810f4d`, media_import `e28b622e` (confirmed with `rev-parse`). Setup inside the worktree: `submodule update --init --recursive`, `setup-addons.sh`, `pnpm install --frozen-lockfile`, `merge-addon-messages.mjs`.

Baseline: 28 files / 374 tests green (`SidebarAddonNav`, `SidebarAddonNavCatalogue`, `components/sidebar/__tests__/*`, `addon-name-neutral-hosts`, `sidebar-headings`, `section-headings`, `i18n-keys`, `popup-dismissal`). The three addon tests the bump adds, run through core vitest (as core CI does via the link tree): 3 files / 11 tests green.

## Q1 — the real bundled declarations, rendered

### Backend catalogue

The test image (`litloft-test-r267b`, built at this SHA) with the two manifests mounted at `/app/addons/{intelligence,knowledge}/manifest.json`, `addons/media_import/backend` mounted as the in-process package `/app/addons/media_import`, `INTELLIGENCE_SERVICE_URL`/`KNOWLEDGE_SERVICE_URL` set (so `is_configured` keeps the external ones), logging at WARNING to stdout. A script imported `app.main` (which runs `_load_addons` -> `register_in_process`), called `addon_registry.load_external_manifests()`, then `await addons_status(drive=None)`.

Observed: no log line at all (so no `navigation ignored` warning), and each addon's `navigation` in the response equals its declaration byte for byte:

- `intelligence` scope drive, href `/drive/{drive}/addons/intelligence`, navigation `{Ask, intelligence.nav.label, message-circle-question, primary, 10}`
- `knowledge` scope drive, href `/drive/{drive}/addons/knowledge`, navigation `{Notes, knowledge.nav.label, notebook-pen, primary, 20}`
- `media_import` type in_process, scope drive, href `/addons/media_import`, navigation `{YouTube & Feeds, mediaImport.sidebar.label, rss, sources, 10}`

The validator accepts all three (both registration paths exercised).

### Sidebar with that catalogue, real next-intl, merged messages

Temporary probe test (deleted afterwards; `git status --short` empty): `Sidebar` with the three entries above verbatim, `vi.mock("next-intl")` replaced by the actual module inside `NextIntlClientProvider` with `src/messages/{en,ja}.json`, drive `家族ビデオ & co`.

en, in order: `Litloft, Home*, Library, Ask (lucide-message-circle-question-mark), Notes (lucide-notebook-pen), [Views] Favorites, Liked, Recently Viewed, Recently Added, All Files, [Sources] YouTube & Feeds (lucide-rss), [Collections/Pins] pin, Trash, Missing Files, [Administration] Dashboard`.
ja: `ホーム*, ライブラリ, 質問, ノート, [ビュー] …, [ソース] YouTube とフィード, …`.

Every link is the core-built `/drive/%E5…co/addons/{name}`; media_import's manifest `href` (`/addons/media_import`) is not used. Placement, priority order, Sources heading only over the sources row, labels from each addon's own key in both locales (not the raw key, not `AddonMeta.label`, not `navigation.label` in ja): invariants 2, 4, 5, 6, 7 hold for the real shape.

Keys: `intelligence.nav.label`, `knowledge.nav.label`, `mediaImport.sidebar.label` are present in merged `src/messages/{en,ja}.json` and absent from `src/messages-core/{en,ja}.json` (node read of both trees); in each addon they live under `frontend/messages/` (diffs below). Translation file policy holds.

Lighting (lit set over every row, en):

| pathname | lit |
|---|---|
| `/drive/%E5…co/addons/intelligence` | `[Ask]` |
| `/drive/家族ビデオ & co/addons/intelligence/find` (real deep page, `pages/find.tsx`) | `[Ask]` |
| `/drive/%E5…co/addons/intelligence/pickup` (real deep page) | `[Ask]` |
| `/drive/家族ビデオ & co/addons/knowledge` | `[Notes]` |
| `/drive/%E5…co/addons/media_import` | `[YouTube & Feeds]` |
| `/addons/media_import` (drive-scoped addon; `app/addons/[name]` 404s it) | `[]` |

Exactly one lit row on each addon route; invariant 8 holds on the real routes. Knowledge has no `pages/` so it has no deep route today; its in-addon links (`?edit=`) keep the pathname and so keep Notes lit.

## Q2 — what else the bump brings in

`git log --oneline --first-parent <old>..<new>`: exactly one commit per addon, each the P2-4 merge, each on `origin/main`:

- intelligence `9c084f4..c4fe4a4`: `Merge pull request #67 feat/navigation-declaration` — `manifest.json` (+navigation), `frontend/messages/{en,ja}.json` (+`intelligence.nav.label` Ask / 質問), `frontend/navigationLabel.test.ts`, `tests/test_manifest_navigation.py`.
- knowledge `07c59e5..95810f4`: `Merge pull request #40 feat/navigation-declaration` — `manifest.json` (+navigation), `frontend/messages/{en,ja}.json` (+`knowledge.nav.label` Notes / ノート), `frontend/__tests__/manifest.test.ts`. `origin/main` HEAD is `95810f4`, so P2-5 (folder New note) is not merged and not included — S2-13 respected.
- media_import `37a20cc..e28b622`: `Merge pull request #16 feat/navigation-declaration` — `backend/router.py` `ADDON_META` (+navigation), `frontend/messages/{en,ja}.json` (+`mediaImport.sidebar.label`), `frontend/navigationLabel.test.ts`, `backend/tests/test_addon_navigation.py`. `origin/main` has `09a4407` (#17, Import from URL Add row) after it; the pin stops at `e28b622`, so that row is not included — S2-12 ("pin to the P2-4 merge, not main HEAD") and S2-15 respected.

No slot, component, `slots.ts`, route or backend handler changed in any addon. Catalogue slots at the bumped pointers: `folder-actions-menu` = `[folder-ai-actions (intelligence, "AI")]` only — no New note row, so stage-2 invariant 9 is untouched by the bump. Nothing user-visible beyond the three nav rows and their labels.

## Mutation table

Runner: a script replaced one exact, unique string, printed the unified diff, ran the vitest set, and wrote the original back (asserting byte equality). Vitest set = the baseline 28 files plus the three addon tests the bump adds (385 tests). Addon-repo Python tests were run in the test image with the addon mounted (`--noconftest`), same apply/restore. The validator mutation was applied to a scratch copy mounted over `/app/app/services/addon_registry.py`, so the tree was not touched. After all runs `git status --short`, `git diff --stat` and `git -C addons/{intelligence,knowledge,media_import} status --short` were all empty.

| id | file | mutation | want | got |
|---|---|---|---|---|
| M04 | addonNavigation.ts | tie-break by `navigation.label` instead of `name` | kill | kill (tie-by-name test) |
| M05 | addonNavigation.ts | `href === null` skip -> fallback `/addons/${name}` | kill | kill (no-href, no-drive) |
| M06 | addonNavigation.ts | drive-scoped with no drive and `meta.href` -> `/addons/${name}` | kill | kill (no-drive) |
| M13 | isSidebarLinkActive.ts | decoded prefix without `/` boundary | kill | kill (prefix-name, decoded) |
| M14 | isSidebarLinkActive.ts | `samePath(pathname, href)` -> `pathname === href` | kill | kill (own page, decoded) |
| M27 | Sidebar.tsx | utility rows get raw `close` | kill | kill (utility dismissal) |
| M18 | AddonNavRows.tsx | remove `rss` | kill | kill |
| M19 | AddonNavRows.tsx | remove `notebook-pen` | kill | kill |
| M20 | AddonNavRows.tsx | remove `download` | kill | kill |
| N01 | addonNavigation.ts | tie-break by name descending | kill | kill (2) |
| N02 | addonNavigation.ts | `addonUrlFor(..., currentDrive ?? "x")` | kill | kill (no-drive) |
| N03 | AddonNavRows.tsx | remove `package` | live | live — inert: the fallback is the same `Package` |
| N04 | AddonNavRows.tsx | `message-circle-question` -> `Rss` | kill | kill |
| N05 | Sidebar.tsx | `SidebarLibrarySection` gets raw `close` (primary + sources rows) | kill | kill (primary, sources dismissal) |
| N06 | Sidebar.tsx | gate also requires `currentDrive !== null` | live | live — hides only drive-less global/both rows, which the invariants do not declare (r1 F6, already filed) |
| N07 | isSidebarLinkActive.ts | drop encoded `${href}/` prefix branch | kill | kill |
| B1 | addons/knowledge/manifest.json | navigation `priority` 20 -> `"20"` (validator would drop Notes) | kill | kill (knowledge `manifest.test.ts`, run by core vitest) |
| B2 | addons/knowledge/frontend/messages/ja.json | `ノート` -> `Notes` | kill | kill (knowledge `manifest.test.ts`) |
| B3 | addons/intelligence/frontend/messages/en.json | `nav` -> `navi` | kill | kill (intelligence `navigationLabel.test.ts`, core `i18n-keys` addon parity) |
| B4 | addons/intelligence/manifest.json | `i18n_key` -> `intelligence.nav.labl` | kill in addon CI, live in core | core vitest live (385); intelligence `tests/test_manifest_navigation.py` kill |
| B5 | addons/media_import/backend/router.py | placement `sources` -> `source` | kill in addon CI, live in core | core vitest live (385); media_import `backend/tests/test_addon_navigation.py` kill (2: literal + core validator) |
| V1 | backend addon_registry.py (scratch copy) | reject `priority >= 20` (would drop real Notes) | kill | kill (55 in core `test_addon_navigation.py`; baseline 93 passed) |

Applied diffs (printed by the runner before each run), representative:

```diff
# M04
-        (a.name < b.name ? -1 : a.name > b.name ? 1 : 0),
+        (a.navigation.label < b.navigation.label ? -1 : a.navigation.label > b.navigation.label ? 1 : 0),
# M06
-      return href === null ? [] : [{ name, navigation, href }];
+      const h = href ?? (meta.href && !currentDrive ? `/addons/${name}` : null); return h === null ? [] : [{ name, navigation, href: h }];
# M14
-  if (samePath(pathname, href)) return true;
+  if (pathname === href) return true;
# M27
-      <AddonNavRows entries={utilityAddons} linkClass={linkClass} close={closeIfOverlay} />
+      <AddonNavRows entries={utilityAddons} linkClass={linkClass} close={close} />
# B4
-    "i18n_key": "intelligence.nav.label",
+    "i18n_key": "intelligence.nav.labl",
# B5
-        "placement": "sources",
+        "placement": "source",
# V1
+    if priority >= 20:
+        return "priority out of range"
```

B4/B5 surviving core CI is expected, not a gap: the declarations live in the addon repositories, and the pinned commits are the merges whose own suites kill both. The core test image carries no addons by design.

## Q3 — did `49fd717f` do what round 1 asked

Yes. All nine r1 survivors are killed, each by the test the commit added for it: M04 (F1), M05 and M06 (F2), M13 and M14 (F3), M27 (F4), M18/M19/M20 (F5). The commit touches only `SidebarAddonNav.test.tsx`. The added `currentDrive` mock variable is reset in `beforeEach`. The no-drive test also sets `catalogueDrive = null`, so the gate passes and the render reaches `addonNavEntries` (M06 and N02 prove it). The prefix-name tests now assert the full lit set (`toEqual([])`) instead of `not.toContain`, as r1 asked. The `package` token row cannot be told apart from the fallback (N03); that is inherent, not a gap.

## Q4 — trajectory

The fix commits so far are `49fd717f`, and `7188d81a` is the behaviour change for this round. Read in order:

- `749dc9a2` (round 1 subject) added the production branches: placement filter, sort, href-null skip, catalogue-drive gate, prefix lighting.
- `7188d81a` adds data only: three `navigation` blocks and their message keys. It adds no core branch, state or prediction, and the addon diffs change no slot, component or route.
- `49fd717f` adds tests only. No production branch, state or prediction.

No round after the first added a branch, a state or a prediction, so the pattern the question asks about is absent.

## Findings

None. With the bump, every invariant in `267-invariants.md` (including the r1 revision to 6) holds for the real bundled declarations and routes, as shown above. The bump brings in nothing beyond the three declarations, their messages and the addons' own tests. The round-1 A findings are closed.

Already filed, one line each: r1 F6 (addon rows off-drive; N06 is the same surface), F7, F8, F9, F10 — [pre-existing to this round] unchanged by `7188d81a`/`49fd717f`, not re-derived.

TOTAL: 0 findings
