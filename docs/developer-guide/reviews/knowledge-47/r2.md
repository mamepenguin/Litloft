# knowledge #47 レビュー R2 — fix `e449e54898849e88ca46b1ae4834d6f9959d16e6`（親 `ddb7563`）

ツリーは scratch に tarball から構築（core `fff3001b` + knowledge `e449e54` を実ディレクトリで `addons/knowledge`、別コピーに `ddb7563`）。`setup-addons.sh`、`pnpm install --frozen-lockfile`、`scripts/merge-addon-messages.mjs` 実行。worktree 不使用、作者ツリー不接触。

## ベースライン（e449e54）

- `pnpm exec vitest run src/addons/knowledge`: 39 files / 901 tests pass（r1 の 898 + 新規 3）
- `pnpm exec tsc --noEmit`: エラーなし
- 新規テストを `ddb7563` の `ConnectionsGraph.tsx` に当てると item 8 の 2 テストが fail（2 failed / 11 passed）。作者報告と一致。`NotesPage.tsx` と `pages/connections.tsx` は `ddb7563` と `e449e54` で同一（`diff` で確認）なので、F1/F2 のテストは実装の変異で確かめた（N1〜N3）

## Trajectory

**この fix は取り除いただけで、何も足していない。** 主張どおり。

- `diff -w ddb7563 e449e54 -- frontend/ConnectionsGraph.tsx` の差分は削除行だけで、追加行はない。生の diff 313 行の残りはインデント変更だけ。
- 取り除かれたもの:
  - state `open`（`useState(true)`）
  - コントロール: `aria-expanded` 付きのトグル `<button>` とラベル `t("title")`
  - 分岐: `ChevronDown` / `ChevronRight` を切り替える三項演算子
  - 条件つきラッパー: `{open && (<div className="flex flex-col gap-3">…</div>)}`
  - 使われなくなった import 2 つ
- ラッパーは置き換えられず、なくなっただけ。子要素は外側の `<section className="flex flex-col gap-3">` の直下に移った。条件式は、`loading`、`error`、`!loading && !error && !hasAnyContent`、`hasGraph`、`truncated` / `TOO_BIG_THRESHOLD && !focusedId`、`selectedNode`、`legendItems.length`、orphan の `orphan_count > 0` / `orphansOpen && data` のすべてで一字も変わっていない（`diff -w` に現れない）。
- `useShortcuts` の有効化フラグ（`focusedId !== null || selectedId !== null`）と、ズーム用 window リスナーには、もともと `open` への参照がなかった。変わっていない。
- テスト側で増えたのは 3 テストと、テストファイル内の可変変数 `drive` と `next/link` モック。本番コードには branch も state も予測も増えていない。
- 残骸は 1 つある。翻訳キー `knowledge.connections.title` を読むコードがなくなった（R4）。

## Findings

### R1 [introduced-by-fix] 不変条件 8 — item 8 のテストは `aria-expanded` 付きのコントロールしか数えておらず、この SHA ではその数が 0

- 観測:
  - `e449e54` の `ConnectionsGraph.tsx` と `frontend/graph/*`（`GraphControls` / `GraphPanels` / `GraphLayers`）に `aria-expanded` と `<details>` は 1 つもない（grep で 0 件）。graph/ はコアのコンポーネントを import していない。
  - つまり 2 つのテストの `for (… querySelectorAll("[aria-expanded]"))` はどちらのフィクスチャでも 0 回しか回らない。数が 0 でないことも表明していない。
  - ループを両テストから削除しても 13 pass のまま（T1、want=live、そのとおり）。いまのテストが保持しているのは、実質「グラフまたは空状態が描かれる」ことだけで、既存の `renders nodes and edges from API` / `shows empty state …` と重なる。
- 殺せるのは、`aria-expanded` を付けてトグルを戻す変異だけ（G2、kill）。
- 生き残る変異:
  - G1（want=kill）: `aria-expanded` を付けない素の「Hide」ボタンで本文全体を畳む → 13 pass
  - G4（want=kill）: 同じトグルを `aria-pressed={hidden}` にする → `src/addons/knowledge` 全体で 901 pass
  - G1 の差分:
    ```diff
    +  const [hidden, setHidden] = useState(false);
    ...
         <section className="flex flex-col gap-3">
    +      <button type="button" onClick={() => setHidden((v) => !v)}>Hide</button>
    +      {!hidden && (<>
    ...
    +      </>)}
         </section>
    ```
- 観測できるフィクスチャはある。`baseGraph` には orphan トグル（`aria-expanded` なしの button）があるので、「本文内の button を全部押す」ようにすれば押す対象は 1 以上になる。その数が 0 でないことを表明し、押したあとにグラフの svg が残ることを確かめれば、G1 と G4 を殺せる。
- 利用者への影響: 今のコードに問題はない。ただ、1 回の操作でページ本文を空にするコントロールが戻っても（不変条件 8 違反）、`aria-expanded` を付けていなければ CI を通る。R-4「A を通すテストは A」に当たる。

### R2 [introduced-by-fix] 不変条件 8 — 失敗状態（`error`）を空にできないことは、どのテストも保持していない

- 不変条件 8 は本文を「グラフ、または空状態、または失敗状態」と定義している。新しい 2 テストのフィクスチャはグラフありと空だけ。
- 変異 G3（want=kill）: 失敗状態のときだけ `aria-expanded` 付きのトグルを出し、押すとエラー表示を消す。結果は 13 pass（生存）。
  ```diff
  +  const [open, setOpen] = useState(true);
  ...
  -      {error && (
  +      {error && <button type="button" aria-expanded={open} onClick={() => setOpen((v) => !v)}>Hide</button>}
  +      {error && open && (
           <p className="text-xs text-danger" role="alert">
  ```
- R1 と違い、`aria-expanded` を付けても生き残る。既存の `shows error message when API fails` はクリックしないし、item 8 のテストは失敗状態を描かない。
- 観測できるフィクスチャ: 既存テストと同じ 500 応答のモックで描画し、コントロールを押したあとも `role="alert"` が残ることを見ればよい。
- 利用者への影響（退行した場合）: 読み込みに失敗したページで、エラー文を 1 回の操作で消せてしまい、見出しと戻るリンクだけの空ページになる。

### R3 [introduced-by-fix] 不変条件 8 — ページ側（`pages/connections.tsx`）で本文を畳むコントロールが追加されても、どのテストも落ちない

- 不変条件 8 の主語は「connections ページの本文」。一方、fix が加えたテストは `ConnectionsGraph` 単体しか描画しない。`connectionsPage.test.tsx` はグラフをモックしていて、クリックもしない。
- 変異 G5（want=kill）: ページ側に `aria-expanded` 付きのトグルを置き、グラフのラッパーを `{open && …}` で包む。`src/addons/knowledge` 全体で 901 pass（生存）。
  ```diff
  +import { useState } from "react";
  ...
  +  const [open, setOpen] = useState(true);
  ...
  -      <div className="px-4">
  +      <button type="button" aria-expanded={open} onClick={() => setOpen((v) => !v)}>{t("connections")}</button>
  +      {open && (<div className="px-4">
           <ConnectionsGraph drive={drive} />
  -      </div>
  +      </div>)}
  ```
- r1 の F3 で問題になった形（ページ見出しと同じラベルのトグルで本文を空にする）が、置き場所をページに移すだけで CI を通る。
- 観測できるフィクスチャ: `connectionsPage.test.tsx` で、グラフのモック（`data-testid="graph"`）を描いたまま、ページ内の button を全部押し（数が 0 でないことを表明）、`graph` が残ることを見る。いまはページに button がないので、表明する数はリンクを除いた「ページの操作要素」など、定義を決める必要がある。
- 利用者への影響（退行した場合）: F3 と同じ。

### R4 [introduced-by-fix] 不変条件: なし — 翻訳キー `knowledge.connections.title` を読むコードがなくなった

- `ddb7563` でこのキーを読んでいたのは `ConnectionsGraph.tsx:299` の `t("title")`（名前空間 `knowledge.connections`）だけ。`e449e54` の `ConnectionsGraph.tsx`、`graph/*`、その他のアドオン frontend に、この名前空間で `title` を引く箇所はない（grep で確認）。core 側で `knowledge.connections` を参照するのは、アドオンのファイルと mockups の html だけ。
- 一方、`messages/en.json` / `ja.json` にはキーが残っている（値はページ見出しの `knowledge.notes.connections` と同じ文言）。
- `i18n-keys.test.ts` は、ja と en のキーの一致、コアのキーとの衝突、アドオン間の重複しか見ておらず、使われていないキーは検出しない。
- 利用者への影響: なし。バケットは B。

## Mutation / probe table

| id | 対象 | 変更の要約 | want | result | verdict |
|---|---|---|---|---|---|
| P0 | ddb7563 の ConnectionsGraph + e449e54 のテスト | 親の実装に新テストを当てる | kill | 2 failed / 11 passed | 作者報告と一致 |
| P1 | `diff -w` ConnectionsGraph | 条件式の保存を確認 | — | 差分は削除行だけ | Trajectory 参照 |
| P2 | grep `aria-expanded` / `<details` | e449e54 の graph 本体と graph/ | — | 0 件 | R1 の根拠 |
| T1 | ConnectionsGraph.test | item 8 の 2 テストから `[aria-expanded]` ループを削除 | live | 13 pass | 数えている集合が空であることの確認 → R1 |
| G1 | ConnectionsGraph | `aria-expanded` なしの素の「Hide」ボタンで本文全体を畳む | kill | 13 pass（生存） | R1 |
| G2 | ConnectionsGraph | `aria-expanded` 付きトグルで本文全体を畳む（元の形に戻す） | kill | 2 failed | OK |
| G3 | ConnectionsGraph | 失敗状態だけを畳む `aria-expanded` 付きトグル | kill | 13 pass（生存） | R2 |
| G4 | ConnectionsGraph | `aria-pressed` のトグルで本文全体を畳む（knowledge 全体を実行） | kill | 901 pass（生存） | R1 |
| G5 | pages/connections.tsx | ページ側の `aria-expanded` トグルでグラフのラッパーを畳む（knowledge 全体を実行） | kill | 901 pass（生存） | R3 |
| N1 | NotesPage | href から `encodeURIComponent` を外す | kill | 1 failed（encoding テスト） | F1 の修正は有効 |
| N2 | NotesPage | `encodeURIComponent` → `encodeURI` | kill | 1 failed / 900 passed | `#` と `?` で区別できている。OK |
| N3 | NotesPage | `<Link>` → `<a>`（閉じタグも） | kill | 1 failed / 900 passed | F2 の修正は有効 |
| T2 | NotesPage.test | `next/link` モックを削除 | 落ちるのは `data-next-link` の表明だけ | 1 failed / 24 passed（`the connections entry` のみ） | モックはほかのテストの観測を変えていない。href だけを見るテスト（Note r1、allLink、back など）はモックがあってもなくても pass |
| P3 | NotesPage.test | `drive` を戻す場所 | — | トップレベルの `beforeEach` で `drive = "d"`。encoding テストはファイルの最後の describe | テスト間で値が漏れる経路はない |
| P4 | 不変条件 3 | loading / error / empty / graph の各条件 | — | P1 のとおり不変 | OK |

注（finding ではない、[pre-existing]）: `+` / `-` / `=` のズーム用リスナーは `window` に張られていて、ツールバーの検索欄に「-」を入力してもズームアウトする（コードを読んで判断した。入力先を確かめる分岐がない。実行して確かめてはいない）。`ddb7563` 以前からあり、この fix は触っていない。

全変異のあと、pristine コピーとの `diff -r` で差分がないことを確認済み。

TOTAL: 4 findings
