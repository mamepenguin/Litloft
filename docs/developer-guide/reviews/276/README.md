# PR #276 — the Enter that confirms an IME conversion

One round. `invariants.md` is the list it was briefed with, including the
supervisor's revision after round 1. Round 1 found no user-reachable defect; its
findings were test gaps, fixed with tests only, so no second round was run.

Read the code for what the system does now. These files quote it as it stood at
the SHA the round reviewed.
