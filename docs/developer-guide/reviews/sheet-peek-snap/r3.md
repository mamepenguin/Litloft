# Review: sheet-peek-snap v2, round 3

- Commit under review: `a193b7cf` (fix commit), parent `5fbfc1c3`. Branch `feat/sheet-peek-snap`.
- Worktree: `/Users/libre/Sources/video_share/.claude/worktrees/feat-sheet-peek-snap`, HEAD `a193b7cf`, clean at start.
- Record read: `invariants.md`, `README.md`, `r1.md`, `r2.md`; diffs of `5fbfc1c3` and `a193b7cf`.

## Q1. Is the removal complete and clean?

- `git diff b1aded11 a193b7cf -- FileDetailShell.tsx VideoPlayer.tsx` is empty: both files are byte-identical
  to `b1aded11`. Right, because the only change `5fbfc1c3` made to them was the `CollapseSheetContext`
  provider wrapper (plus re-indentation) and the collapse effect, both of which the user had removed.
- `grep -rE 'CollapseSheet|collapseSheet'` outside `docs/developer-guide/reviews/`: only the i18n key
  `inspector.collapseSheet` and its two uses in `FileDetailChrome.tsx`, which already existed at `2957e640`
  (the toggle's label) and are unrelated to the removed context. No addon references it (it never had one).
- Prose describing the removed behaviour: DESIGN.md bullet removed; user-guide sentence "Sending the video full
  screen lowers the sheet first" removed; no other hit for full screen + lower/collapse in `docs/`, `DESIGN.md`,
  `frontend/src`, `frontend/e2e-*`, `addons/`.
- Tests of the removed behaviour: VideoPlayer "lowers the file sheet when it pins the frame", MediaShell
  "gives the page a way to lower the raised sheet", MobileInspectorSheet "collapsing the sheet from inside the
  page" — all deleted, with their now-unused imports (`SHEET_PEEK_PX`, `useCollapseSheet`, `FileDetailShell`,
  `CollapseSheetContext`).
- `5fbfc1c3`'s other intended fixes kept, per `git diff b1aded11 a193b7cf`: in-sheet dialog host removed
  (`MobileInspectorSheet.tsx`, the unit test now asserts `parentElement === document.body`); `DialogPortal`
  docstring about modal vaul removed; `e2e-components/fixtures/app.tsx` `SheetOverPage` arrangements and
  `componentFixtureParity` entries kept; browser cases for the inset, the tier and page scroll kept; the
  user-guide "the page scrolls" clause kept; DESIGN.md dialog bullet kept; known-issues entry for Escape kept.
- r2 G6: the constant `state` assertion is removed; the case still asserts `scrollTop > 30`.
- New known-issues entries record r2 G1 (in-place addon dialogs) and G2/G3 (full-screen stacking,
  YouTube included).

## Findings

### H1. Invariant 8 (changing files resets the sheet) is still held by no test

- Label: `[pre-existing]`. The reset effect (`FileDetailShell.tsx`, `useEffect(() => setSheetState(SHEET_STATE_PEEK), [resetKey])`)
  comes from `a76c6d41`, before this branch, and `FileDetailShell.tsx` at `a193b7cf` is identical to `b1aded11`.
  Round 1 already noted it ("no test that changing files resets the sheet"); this line only confirms it at HEAD.
- Invariant: 8 (test gap; the code holds it). Bucket B.
- Reproduction: M4 below (the `setSheetState` call removed) leaves all shell-related Vitest files green (15 files, 349 tests).

### Already recorded, not re-derived (one line each)

- r2 G1 `[pre-existing]`, invariant 9 for in-place addon dialogs (`KnowledgeSaveDialog`, `FileLinkModal`): unchanged; now in `known-issues.md`.
- r2 G2/G3 `[pre-existing]`, pseudo-fullscreen under header/strip/sheet (VideoPlayer and YouTube): with the collapse removed, the raised sheet also covers the frame again, as on `b1aded11`; now in `known-issues.md`.
- r2 G4 `[pre-existing]` relative to this commit: M2 below, killed by the DOM-shape unit test only; 52/52 browser green.
- r2 G5 `[pre-existing]` relative to this commit: M5 below, killed by MediaShell unit tests only; 52/52 browser green.

## Q2. Invariants 1-11 at `a193b7cf`

The fix touches no modal flag, tier, strip, gesture, reset or reserve code; the shell and player are back to `b1aded11`,
and the sheet keeps `5fbfc1c3`'s host removal.

| # | Holds? | Evidence at `a193b7cf` |
|---|---|---|
| 1 | yes | M1 (`modal`) killed: 3 unit ("leaves the page exposed, operable and undimmed" half/full, dialog case) + 3 browser |
| 2 | yes | M1 killed by browser "takes a tap, and the sheet stays where it is" |
| 3 | yes | not touched; r1 M16 kills stand (drawer code unchanged since `5fbfc1c3` apart from host removal) |
| 4 | yes | layout suite 143/143; M5 killed (unit only, r2 G5) |
| 5 | yes | M1 also killed "is covered by an overlay sidebar opened over it"; M3 killed unit tier test |
| 6 | yes | not touched; unit files green |
| 7 | yes | sheet-gesture 52/52 green; gesture code not touched |
| 8 | yes in code, untested | H1 / M4 |
| 9 | for hook callers yes (dialog lands on `body`); not for in-place addon dialogs | M2 killed by unit only; r2 G1 in known-issues |
| 10 | yes | M1 killed by "leaves the page scrollable", still after the G6 assertion was dropped |
| 11 | yes | M3 (`z-[5]`) killed by browser "is drawn over the page's header and its sticky player" |

Baseline at `a193b7cf`: `pnpm typecheck` clean; eslint `0 errors, 63 warnings` (the unused import is gone);
`pnpm test` 497 files / 7251 passed; touched Vitest files 191/191; `playwright-components` sheet-gesture +
popup-dismiss 52/52; `playwright-layout` `mobile` pattern 143/143.

## Q3. Trajectory

Read in order:

- `5fbfc1c3` removed a state (the in-sheet dialog host) and added a branch plus a prediction (the
  `pseudoFullscreen -> collapseSheet()` effect, carried by a new `CollapseSheetContext` channel: "a player that pins
  itself must lower the sheet").
- `a193b7cf` only removes: the context, its hook, the shell's provider wrapper and callback, the player's effect,
  the three tests of that behaviour, the DESIGN.md bullet and the user-guide sentence that predicted it, the constant
  `state` assertion (r2 G6), and an unused import. What it adds is prose only: two `known-issues.md` entries and the
  review records. No branch, state or prediction is added; source files return to `b1aded11` byte for byte except the
  kept host removal.

So this round does not repeat the previous round's shape. It removes the prediction the previous round added, and the
remaining delta over `b1aded11` (host removal, browser cases) is itself a removal plus tests. The chain is converging.

## Mutation table

`unit` = the Vitest files named per row; `comp` = `playwright-components` `sheet-gesture` + `popup-dismiss` (52 cases).
Each mutation applied alone and the file copied back from a saved original; `git status` checked empty after each.

| # | Mutation | want | unit | comp | Killed by |
|---|---|---|---|---|---|
| M1 | `MobileInspectorSheet`: `modal={false}` -> `modal` | kill | kill | kill | unit: exposed/operable half+full, dialog case; comp: "leaves the page scrollable", "takes a tap…", "covered by an overlay sidebar" |
| M2 | `MobileInspectorSheet.tsx` replaced by its `b1aded11` version (in-sheet dialog host back) | kill | kill | **live** (52/52) | unit "sends dialogs opened from inside it to the page" only (r2 G4) |
| M3 | sheet `z-[25]` -> `z-[5]` | kill | kill | kill | unit "sits above the page's chrome…"; comp "is drawn over the page's header and its sticky player" |
| M4 | shell: `setSheetState(SHEET_STATE_PEEK)` removed from the `resetKey` effect | kill | **live** (349/349) | n/r | none (H1) |
| M5 | shell: phone `paddingBottom: SHEET_PEEK_HEIGHT` -> `undefined` | kill | kill | **live** (52/52) | MediaShell "ends the page above the strip…", fixture parity (r2 G5) |
| M6 | shell: `data-sheet-snap` always `"peek"` on phone | kill | kill | n/r | MediaShell "publishes where the sheet is, for the player to read in CSS" |
| M7 | `FileDetailShell.tsx`, `VideoPlayer.tsx`, `MobileInspectorSheet.tsx` replaced by their `5fbfc1c3` versions (collapse channel back, tests at HEAD) | live | live (340/340) | live (52/52) | expected: the user removed the behaviour and nothing asserts its absence |

`n/r` = not run for that row. `git status --short` empty at the end, HEAD `a193b7cf`. Known flakes not run.

TOTAL: 1 findings
