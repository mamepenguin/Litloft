# PR-2 review, round 6 — `aa907c07b`

Worktree `<review-worktree>`, detached at `aa907c07b`.
Baseline: vitest 270/270 passed; playwright-epub 75 passed, 3 skipped (Chromium + WebKit).

## Mutation table

(filled in as run; every mutation restored with `git checkout -- <file>`)

| id | mutation | want | vitest | playwright (Chromium + WebKit) | result |
|---|---|---|---|---|---|
| M1 | `served = type === HTML ? charset : type` (charset only for HTML, the `a0f0bb38e` value) | kill | FAIL(40), exact type strings | FAIL(2): new "stale XML encoding declaration" test, both engines | killed |
| M1b | M1 + that test's type assertion relaxed to `split(";")[0]` | kill | – | FAIL(2): `Expected substring "縦書き"`, `Received ""`, both engines | killed by the rendered text alone, not only by the string compare |
| M2 | `served = (type) => type` (no charset anywhere) | kill | FAIL(41) | FAIL(10): the XML-declaration test and the four "mistakes are shown" tests, × 2 engines | killed |
| M3 | `transformResource`: `if (type === SVG) { detail.type = type; return; }` (SVG sections unsanitized) | kill | FAIL(1) "image/svg+xml is sanitized" | FAIL(2): "runs no script with the sanitizer alone", `__pwned = ["svg-doc","svg-doc-onload"]`, Chromium and WebKit | killed through the new walk, in both engines |
| M4a | walk `still < 1` (stop at the first turn that does not move) | live | – | 12/12 hostile pass | live, as declared: the SVG section loads on the turn that *does* move (see trace) |
| M3+M4a | both | kill | – | FAIL(2), same `__pwned` both engines | killed: the second still turn is margin, not load-bearing |
| M4c | walk: `if (before.index === indexOf("c4")) break;` right after `reached.add` (stop one turn early: no turn past c4) | live | – | 12/12 pass | live, as declared (test-only; returned set unchanged) |
| M3+M4c | both | live (browser) | – | 12/12 pass | live: the turn past c4 is exactly what kills M3 in the browser, and nothing in the walk's return value witnesses that it happened. Only an artificial break placed after `reached.add` produces this, because index 5 enters `reached` only when a turn is issued from it |
| M5 | `sanitizeMarkup`: `if (first.type === SVG) return REFUSED` | kill | – | FAIL(4): full + sanitizer-alone, both engines, `reached` gains index 6 | killed by the exact set `upTo("c4")` (the refused XHTML stand-in is laid out) |

Every mutation was restored with `git checkout`. The throwaway spec `e2e-epub/zz-r6.spec.ts` was deleted, and `git status` is empty at `aa907c07b`.

### Probes (not mutations)

- **Walk trace** (a wrapped `transformResource` that logs section names; both engines). Full config: 8 turns. The 6th turn goes from `[index 5, page 1/3]` to `[5, 0/1]`, loads `OEBPS/evil.svg` through the sanitizer, and afterwards `renderer.getContents()` is `[6, "svg", "image/svg+xml", "UTF-8"]`. `lastLocation` still reports index 5, so the walk counts the turn as moving because the page changed, not the index. Two still turns follow.
- **Policy alone.** WebKit: the same 8 turns, and the SVG section *is* loaded (index 6, root `svg`). Chromium: after c3, one turn loads c4 (`application/x-unknown`) as a `text/html` document at index 5, `lastLocation` stays at 4, the walk stops, and the SVG section is never loaded.
- **Blob type with and without `; charset=utf-8`** (iframe of a blob, the way `createURL` makes one; both engines). For XHTML and SVG alike: `contentType` has no parameter (`application/xhtml+xml` / `image/svg+xml`), `characterSet` is UTF-8, the root is `html`/`svg` in its namespace, there is no `parsererror`, the document stays an XML document (`createElement("P").localName === "P"`, `<div/>` has no children), and the text is identical. The parameter changes nothing observable.
- **Slow section.** A section's transform was delayed with the sanitizer alone:
  - c2 delayed by 1000 ms: the walk stops at `[0,1,2]`. That fails `toEqual(upTo("c4"))` and the policy-alone prefix, so it fails loudly (a flake) and cannot pass falsely.
  - The SVG section left unsanitized and delayed by 700 ms: `__pwned` is set by the assertion, so it is caught.
  - Delayed by 1500 ms: the walk returns `upTo("c4")` and `__pwned` is **null** when asserted. It becomes `["svg-doc","svg-doc-onload"]` 2.5 s later. See F2.

## Q1. Did `aa907c07b` do what round 5 asked?

- **R5-F1 (stale XML encoding declaration): closed.** M1 and M2 are killed in both engines. M1b shows that the rendered-text assertion kills the mutation on its own. The unit changes are only the exact type strings.
- **R5-F2 (policy-alone walk never opens the SVG section): closed in WebKit, still open in Chromium.** The new walk loads the SVG section with the policy alone in WebKit, but not in Chromium, because Chromium's paginator stops at the unsanitized c4. The test's own comment says this, and its assertion (`slice(0, 5)`) holds neither outcome. See F1.
- **The SVG coverage of the sanitizer-alone test survives the removal of `openSvgSection`**: M3 is killed in both engines, and M5 is now killed by the exact set rather than by `loaded === "svg"`.

## Q2. What did it break?

- **Charset on XHTML and SVG blobs: nothing found.** foliate uses the served type only as the `Blob` type (`createURL`). The paginator's `data` listener compares against `text/css`, which is untouched. `loadDocument`/`createDocument` parse with `item.mediaType`. `reader.js` and `core.js` do not read the served type. In the iframe, both engines drop the parameter from `contentType` and parse as XML exactly as before (see the probe). Full suites are green: vitest 270/270, and playwright 75 passed with 3 skipped.
- **Stop rule.** The rule can stop early on a slow section (`still` counts a 300 ms wait with no movement). Where that cuts the set of sections, the exact-set assertions fail loudly. What it cannot do is wait for the last load: see F2.

## Findings

### F1. With the policy alone, the SVG section is still never loaded in Chromium, and neither engine is held

- `[pre-existing]`: this is the residue of R5-F2. `aa907c07b` improves it: WebKit now loads the section, which it did not at `a0f0bb38e`. Invariant **1** ("either one alone"). Severity **Low** (a detector gap; no product mutation found that only this would kill).
- The policy-alone assertion is `reached.slice(0, 5)`, so nothing notices if WebKit stops loading the SVG section either. Ledger item.

### F2. The walk's last load is awaited by a fixed timeout, not by the load

- `[introduced]`: at `a0f0bb38e`, `openSvgSection` awaited `section.load()` and the frame's `load` event before asserting. The new walk turns past c4 and then relies on two 300 ms still turns plus a 500 ms sleep. Invariant **1** (sanitizer-alone detector). Severity **Low**.
- Observation: with the SVG section unsanitized and its transform delayed by 1500 ms, the sanitizer-alone test's assertions pass (`reached = upTo("c4")`, `__pwned = null`), and the scripts run afterwards. At 700 ms it is caught. Today the load is immediate (under 10 ms after the turn), so the margin is about 1.1 s. This is detector rule 3 ("wait for the thing you assert on"). The trace shows what could be waited on: `renderer.getContents()[0].index === 6`. That would be a positive witness, and it would also kill M3+M4c. It is not necessary for correctness today.

## Q3. Trajectory

Reading `12fc6fc1a` → `916ba3a72` → `01cee4152` → `a0f0bb38e` → `aa907c07b`:

- **Sanitizer.** `a0f0bb38e` added a conditional keyed to the HTML state (`first.type === HTML ? …charset… : first.type`), the second in a row after `01cee4152`'s `first.type !== HTML` guard. `aa907c07b` **removes it**: `served(type)` is one uniform value, and `REFUSED` uses the same function. This round removes a branch. The `first.type !== HTML` parsererror guard from `01cee4152` remains, and this round did not touch it.
- **Walk detector.** `01cee4152` added the `sections` parameter, `LAID_OUT` and the prediction "the SVG section is never paged into". `a0f0bb38e` added the `if (sections === LAID_OUT)` branch and the `openSvgSection` side path. `aa907c07b` **removes the parameter, the constant, the branch and the side path**, and ends the loop on an observed condition instead of a target index. It adds one piece of state, the `still` counter, which replaces the index-target break rather than adding to it. The prediction "SVG is never laid out" remains, moved from a constant into the exact expected set `upTo("c4")`. That is a declared expectation (detector rule 5), and it is what kills M5. The policy-alone test states an engine-dependent prefix instead of a full set. That is a loosening, not a new branch.
- **Answer.** This round removes more than it adds (−1 conditional in the sanitizer; −1 parameter, −1 constant, −1 branch, −1 side path in the walk; +1 counter). It does not repeat the previous round's pattern. The loop is converging. F2 is the price of dropping the side path's awaited load. Supervisor's call.

## Status

Nothing was fixed, committed or pushed, and no Docker action was taken. The worktree is clean at `aa907c07b`.

TOTAL: 2 findings
