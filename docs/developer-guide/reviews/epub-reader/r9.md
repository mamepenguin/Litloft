# PR-2 review, round 9 — `df4a4b647`

Worktree `<review-worktree>`, detached at `df4a4b647`.
Baseline: vitest 292/292 passed; playwright-epub 83 passed, 3 skipped (Chromium + WebKit).

## Mutation table

(filled in as run; each restored with `git checkout -- <file>`)

| id | mutation | want | vitest | playwright | result |
|---|---|---|---|---|---|
| M1 | remove `[data-sheet-snap] .media-detail-player:has([data-epub-reader]) { position: static }` | live (no suite renders the reader inside the phone shell) | 698 pass (epub + fileDetailShell + `src/app/__tests__` + `components/FileDetail`) | epub 83 pass; layout 306 pass | live, as declared. Nothing holds the fix (F1 below); its effect is measured in the browser instead |
| M2 | remove `data-epub-reader` from `EpubPreview`'s root | live | 698 pass | epub 83 pass; layout 306 pass | live, as declared. Same gap |
| M3 | column switch never back to 1 once 2 (`… !== columns && … !== "2"`) | kill | — | FAIL(2) "on a tall screen, a vertical section after a horizontal one is one page again", both engines (`Expected 1, Received 2` at the column poll, after `index === 2` held) | killed. Round-8 F1 closed |

Every mutation restored with `git checkout -- <file>`; `git status` empty after each. (One layout run under M1 showed 305/306 with `toolbar-menu … sheet form at 500px` failing; two reruns under M1 and the unmutated run were 306/306, so that was a flake unrelated to the rule.)

The running app serves the rule (`.media-detail-player:has([data-epub-reader]){position:static}` found in the served CSS), so it is built from this SHA.

### Runtime M1 (the rule overridden in the running app)

Because M1 cannot reach the running app, the rule was also defeated in the page with an injected `[data-sheet-snap] .media-detail-player{position:sticky!important}` (`r9/collapse_sticky.mjs`). WebKit iPhone 13, 1500 px under the book: scroll 400 + resize → **104 px**, scroll 504 + resize → **0 px**, button not hittable — round 8's F2 reproduces exactly. Without the override (below) it does not. So the rule is what closes F2, and nothing in any suite would notice it going.

## Q1. Round 8 closed?

Scripts: `r9/collapse.mjs` (round 8's reproduction extended with scroll 400/504/800, viewport −20, rotation and back, all while scrolled).

| engine, device | initial | +1500 under | scroll 400/504/800 + resize | viewport −20 scrolled | rotated scrolled | rotated back | at top | removed at top | progress writes |
|---|---|---|---|---|---|---|---|---|---|
| WebKit iPhone 13 | 472 | 320 | 320 / 320 / 320 | 320 | 230 (= room 286 − 56) | 320 | 320 | 472 | 0 |
| Chromium Pixel 7 | 647 | 407 | 407 / 407 / 407 | 395 | 292 | 407 | 407 | 647 | 0 |
| WebKit iPhone 13 landscape | 182 | 182 | 182 / 182 / 182 | 162 | 354 | 182 | 182 | 182 | 0 |

`position` reads `static` on the player box in every step; `top` (the book's offset in the canvas) stays 0 whatever the scroll, so `room` is now computed from a true position. The full-screen button is off screen while the page is scrolled past it (accepted behaviour) and hittable again at the top.

- **Round-8 F2: closed.** No step shrinks the book; heights depend only on canvas size and content, not on scroll.
- **Round-8 F1: closed.** M3 is killed in both engines by the new test, failing at the column assertion after `index === 2` was reached.

## Q2. What did it break?

Measured (scripts `r9/sheet.mjs`, `r9/fs.mjs`, `r9/full.mjs`, `r9/types.mjs`; each also run with the sticky override for comparison where relevant):

- **Other file types.** `:has([data-epub-reader])` matches only the EPUB reader. Phone (WebKit iPhone 13, Chromium Pixel 7): loft video, JPEG, PDF → `sticky`; Markdown has no player box; the broken EPUB (`BfWVHVupCV23`, error state, no attribute) → `sticky`; the working EPUB → `static`. `[data-epub-reader]` count in the document is 0 for every non-EPUB page. Desktop: every type `static` as before (rule is under `[data-sheet-snap]`, phone only). (Progress POSTs were intercepted with a 204 during this run so no view history was written for those files.)
- **Pseudo-fullscreen (WebKit iPhone 13).** At rest, with the sheet at half, and from a page scrolled 150 px (entered with `f`): `data-player-fullscreen` set, frame `[0, 664, 390]` = the viewport, `elementFromPoint` at centre / bottom / top-band is the frame in all three; the root keeps `320px` (the slot holder `::before` is 201 px inside a fixed-height flex column, so nothing under it moves); on exit the frame returns to `[104, 320]` and `scrollTop` is unchanged (0 / 0 / 150). No reader reload, one `/stream` for the whole run (invariant 7), 0 progress writes (invariant 4). The existing `:root[data-player-fullscreen] … { position: static }` rule already made the box static during full screen, so this state is identical to the parent.
- **Chromium Pixel 7** uses native full screen: frame 839 = viewport at rest, half, scrolled, and with the sheet dragged to full (sheet top 168); centre hit is the frame; identical with the sticky override.
- **Mobile sheet.** Rest / half / full (full reached in Chromium with a CDP touch drag): the sheet paints over the book the same way with static and sticky (half top 398 on iPhone 13, 503 on Pixel 7, in both modes). The box keeps `z-index: 10` and, being a grid item, still forms a stacking context; no observable effect found (sheet above it, pinned frame above everything). `useSheetHalfSnap` would have been a second reader of the box's scroll-dependent position, but it is disabled for EPUB (`hasPlayer` is `companionKind !== null`); the half snap stays on the fallback for EPUB in both modes, so no new interaction.
- **`useFillHeight` with the sheet at rest and late content.** Late content at the top: 472 → 320 (iPhone), 647 → 407 (Pixel); removed: back to the initial value. Late content (resize) while scrolled: unchanged (table above). With the sticky override, the same late resize while scrolled shrinks the book (284 at scroll 200 / viewport −20; 0 at 504), so this is where the fix shows.

Not measured:
- WebKit pseudo-fullscreen with the sheet at **full**: mouse drag does not move the vaul sheet in WebKit emulation and WebKit has no CDP touch; only half was measured there.
- A real iPhone (URL-bar collapse while scrolling, physical rotation). Approximated with viewport −20 and a width/height swap while scrolled.

## Findings

### F1. Nothing holds the new rule or the attribute it keys on

- `[introduced]` (the rule and `data-epub-reader` are new in this commit). Invariant **none** (it guards round 8's F2, which was itself invariant-none). Severity **Low**.
- M1 (rule removed) and M2 (attribute removed) pass vitest (698 across epub, fileDetailShell, `src/app/__tests__`, `components/FileDetail`), playwright-epub (83) and playwright-layout (306). Runtime M1 in the running app brings round 8's collapse straight back (104 px, then 0 px with the button unreachable). A rename of the attribute or a refactor of the selector would reintroduce a Medium defect silently. `e2e-layout/` already builds its fixture CSS from `globals.css` and has a mobile-sheet fixture, so a box containing `[data-epub-reader]` under `[data-sheet-snap]` could be measured there (does its top move with the scroll; or does a scrolled resize leave the height unchanged). Supervisor's call whether that is worth it.

No other finding: the pseudo-fullscreen, sheet, fill-height and other-file-type measurements above found nothing that differs from the parent except the intended static box.

## Q3. Trajectory

Reading `969bd523c` (round 8) → `df4a4b647`:

- **Removes:** the stuck state for the EPUB box on a phone, and with it the wrong input (`top` reading as `scrollTop`) that round 7 recorded as a known issue and round 8's cap turned into a collapse. The known-issue entry is removed with it. `useFillHeight` is not touched: the computation that took a term in rounds 7 and 8 takes none in round 9.
- **Adds:** one CSS branch — a type-specific exception to the phone's sticky rule, keyed on a new marker attribute. It is a branch in the stylesheet, but it deletes a state rather than predicting around it; it does not add a guard to `top` or `room`, which is what round 8's trajectory note warned against.
- The test and its assertion add no production branch.
- **Answer.** This round removes the cause instead of patching the computation a third time. It adds one selector branch whose effect is to remove a state. By R-4's test (a round that removes a branch, state or prediction is converging) it reads as convergence, not patching. The one cost is F1: the removal is held by nothing.

## Status

Nothing fixed, committed or pushed; no Docker action. Scripts in the scratchpad `r9/`; worktree `git status` empty at `df4a4b647`. Progress for `4BO4MC4QbR30` not written by these runs (0 progress requests in every EPUB run); other files' progress POSTs were intercepted.

TOTAL: 1 findings
