# PR #280 — the Add menu's Escape and outside press, measured in Chromium

One round. `invariants.md` is the list it was briefed with, including the
supervisor's revisions before review and after round 1. Round 1's fixes were
tests and a deleted comment, so no second round was run.

Read the code for what the system does now. These files quote it as it stood at
the SHA the round reviewed.
