# PR #273 — the markdown file kind becomes text

Two rounds. `invariants.md` is the list they were briefed with, including the
supervisor's revision after round 1. Round 1's fixes were tests plus one removal
(the smart_folders migration); round 2 reviewed those and found no behaviour
defect.

Read the code for what the system does now. These files quote it as it stood at
the SHA each round reviewed.
