# PR #273 review, round 2

Reviewed SHA: `8329f04347ead46fdf312c09c56bf421a14e04fc` (round-1 SHA `54294574`, parent of the PR `96407fd4`).
Fix commits read in order: `d964e2df`, `b4b702f2`, `8329f043`.
Worktree set up with `.cmux/setup`. Backend image `litloft-test-r273b`, rebuilt after every backend mutation.
Tree confirmed clean (`git status --short` empty) before each mutation and after each `git reset --hard HEAD`.

Baseline at the SHA: backend `test_text_kind test_file_kind_filter test_api_folder_tree test_api_drives`
122 passed (r1's 123 minus the deleted migration test). Full backend suite (run once, with a live
mutation applied, see MT7): 2262 passed, 4 skipped.

## Q1: did `8329f043` remove the migration cleanly?

Yes.

- `git diff 96407fd4 8329f043 -- backend/app/database.py` is empty: `database.py` is byte-identical to
  the PR's parent. `text` is still imported and used by the other migrations; no dangling import.
- The deleted test was the only reader of the branch (grep `smart_folders` in `backend/tests`: only
  `test_smart_folders.py`, which never touches `file_type` values other than video/image/xyz).
- `_SMART_FOLDER_FILE_TYPES` and `routers/smart_folders.py` are unchanged against the parent. The only
  write path to `smart_folders.file_type` is `create_smart_folder` / `update_smart_folder`, both through
  `SmartFolderCreate` / `SmartFolderUpdate` validators; `git log -S _SMART_FOLDER_FILE_TYPES` shows the
  set unchanged since `bfb464d7` (introduced together with the table). No MCP tool, addon, or migration
  writes the column. So the premise "the app never writes a `markdown` row" holds for every version.
- Frontend readers: `SidebarSmartFoldersSection.tsx` forwards `sf.file_type` into the search URL, where
  `parseTypeFilter`'s `markdown` alias remains (FM7 below, killed). `SmartFolderSaveButton.tsx` sends
  `typeFilter` (a `FileKind`, so `text` at most), which the backend rejects with 422 and the button shows
  "could not save" — the same behaviour the parent had for `markdown` (r1 F8, recorded; not re-raised).
- No path shows All or a 422 for a value the app itself produces, other than that pre-existing save
  rejection. A hand-edited `markdown` row still opens the search page with Text selected (alias).

## Q2: do the `b4b702f2` tests kill what they claim?

- **r1 M6 (backend `.markdown` suffix):** now killed (MT1, 4 failed). Also killed when the suffix is
  dropped from only the listing query (MT2) or only the folder-card `case` (MT3).
- **Fixture `mime_type=None`:** `File.mime_type` is `NOT NULL, default="application/octet-stream"`.
  A probe test (mounted from outside the tree) read the stored rows:
  `[('legacy.md', 'application/octet-stream'), ('old.markdown', 'application/octet-stream')]`.
  So r1's **M12 (`mime_type IS NOT NULL`) is an equivalent mutation**: no NULL row can exist, and it
  survives at this SHA too (MT4, want=live). It was never evidence of a gap.
- **F3 test against a realisable mutation:** the realisable form of M12 is excluding
  `application/octet-stream` from `document` (MT5) — killed by `test_document_still_holds_every_text_row`
  (and `test_file_kind_filter`). Widening `document` to include `other` (MT6) is also killed by the exact
  equality, which a `<=` would have let through. The test holds invariant 2's "document still returns
  every text row" against both directions.
- **The `old.markdown` row is the only holder of the suffix fallback:** giving it `text/markdown` mime and
  dropping `.markdown` (MT7) survives the whole backend suite — as intended (want=live), confirming the
  None-mime row is load-bearing, not incidental.
- **FM2 (frontend `.markdown` suffix):** now killed by
  `dominantKind.test.ts > counts a .markdown file with no recorded mime as text` (FMT1). The fixture gives
  `mime_type: "application/octet-stream"`, which is what a backend row "with no recorded mime" really
  carries, so the frontend fixture matches the realisable row.
- **FM10 (ja label):** now killed by `oneKindClassifier.test.ts` (FMT2). The en label is asserted in the
  same place.
- **`d964e2df`:** reverting the stand-in type makes `tsc --noEmit` fail at
  `FolderBrowser.folderAnchor.test.tsx(465,34)` (FMT5); CI runs `pnpm exec tsc --noEmit`, so the fix is
  held. vitest does not see it either way.
- The `useCollectionViewMode.ts` docstring deletion is prose only; nothing to mutate.

## Q3: mutation table

Backend runs: `test_text_kind test_file_kind_filter test_api_folder_tree test_api_drives` unless noted
(baseline 122 passed). Frontend runs name their scope; the 2 `file-kind-parity` failures are the known
baseline and are excluded from the counts below.

| id | diff | want | result |
|---|---|---|---|
| MT1 | `_KIND_SUFFIXES["text"] = (".md", ".txt")` | kill | killed (4: `test_text_selects…`, `old_name[text]`, `old_name[markdown]`, folder card counts) |
| MT2 | `_apply_kind_filter`: `... for suffix in suffixes if suffix != ".markdown"` | kill | killed (3, listing/tree) |
| MT3 | `_suffix_kind_case`: `for suffix in suffixes if suffix != ".markdown"` | kill | killed (1, `test_the_folder_card_counts_text_as_the_filter_selects_it`) |
| MT4 | flat branch `query.filter(File.file_type == kind, File.mime_type.isnot(None))` (r1 M12) | live | survived (122 passed). Equivalent: `mime_type` is `NOT NULL` with an octet-stream default; probe shows the None fixtures stored as `application/octet-stream` |
| MT5 | flat branch `..., File.mime_type != "application/octet-stream"` (realisable M12) | kill | killed (2: `test_document_still_holds_every_text_row`, `test_every_kind_selects_its_own_file`) |
| MT6 | flat branch `File.file_type.in_((kind, "other")) if kind == "document" else ...` | kill | killed (2: `test_document_still_holds_every_text_row`, `test_document_holds_text_and_pdf_under_it`) |
| MT7 | test fixture `old.markdown` mime None → `"text/markdown"` **and** MT1 | live | survived, full backend suite 2262 passed / 4 skipped (the None-mime row is the only holder) |
| MT8 | `_normalize_kind` returns `kind` unchanged | kill | killed (2: `old_name[markdown]`, `history[markdown]`) |
| MS1 | restore the removed `else:` migration branch in `database.py` (content of `54294574`) | live | survived (`test_smart_folders test_text_kind` + 4 migration test files: 60 passed). Inert |
| MS2 | `_SMART_FOLDER_FILE_TYPES` adds `"markdown"` | live | survived (`test_smart_folders test_text_kind`: 26 passed). See note under Q1: the premise of `8329f043` is not pinned by a test, but no invariant covers it and no realistic change would add the retired name |
| FMT1 | `textKind.ts` `TEXT_SUFFIXES = [".md", ".txt"]` | kill | killed (`src/lib src/hooks src/components src/__tests__`: 1 beyond baseline, the new `.markdown` test) |
| FMT2 | `messages-core/ja.json` `filter.type.text` "テキスト" → "Markdown" | kill | killed (`oneKindClassifier > names its kinds once…`) |
| FMT3 | search `parseTypeFilter`: `const kind = raw;` | kill | killed (`SearchPage > selects the text filter for type=markdown`) |
| FMT4 | `useTreeKindFilter`: `const kind = raw;` | kill | killed (`reads a filter stored under the old markdown name as text`) |
| FMT5 | revert `d964e2df` (stand-in type without `filename`) | kill | killed by `tsc --noEmit` (TS2353); vitest green either way |

Survivors: MT4, MT7, MS1, MS2 — all declared `want=live`. No `want=kill` survivor.

## Q4: trajectory

Round 1's change (`54294574`) added production branches: the `markdown` query-parameter alias
(`FileKindParam` + `_normalize_kind`), two frontend alias branches (search URL, stored tree filter), the
snapshot discard, and the `smart_folders` migration branch.

This round's fix commits, in order:

- `d964e2df` — a test type annotation. No production change.
- `b4b702f2` — two fixture rows/tests (`old.markdown` backend, `a.markdown` frontend), one assertion
  tightened from `<=` with a carve-out to exact equality, two label assertions, one docstring deleted. It
  adds test predictions that pin existing behaviour; it adds **no** production branch, state or
  prediction.
- `8329f043` — **removes** a production branch (the migration) and its test, and adds nothing.

So this round did not repeat round 1's shape: nothing was added to the production code, and one branch
was removed. The loop is converging, not being patched.

## Findings

### F1 [pre-existing] B — the search page comment still says smart folders carry the old name

`frontend/src/app/drive/[name]/search/page.tsx`, above the alias:
`// Saved URLs and smart folders predate the rename.`
Written in `54294574`; `8329f043` removed the migration on the ground that no smart folder can hold
`markdown` (`_SMART_FOLDER_FILE_TYPES`, MS2 context), but left this line. A reader who takes it at face
value would conclude stored smart folders hold `markdown` and restore the migration just removed (the
code change MS1 shows is inert). The alias itself is correct and must stay (FMT3 killed). Suggested
remedy: delete "and smart folders" (or the comment); do not reword.

## Notes (not counted)

- r1 F8 (saving a Text-narrowed search is rejected with 422 → "could not save") is unchanged and remains
  the only place the app itself produces a value the backend rejects on these surfaces. Recorded in r1;
  not re-raised.
- `useFolderFiles.ts` comment "`file_type` is a column that never holds `markdown` or `pdf`" is still
  true.
- The frontend `.markdown` test uses `file_type: "document"`; the r1 F7 guard question
  (`useCollectionViewMode` requires `document`) is untouched by this round.

TOTAL: 1 findings
