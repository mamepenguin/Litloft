# knowledge P2-5 r2 — New note Add menu row (after the clip-row removal)

Reviewed: addon `knowledge` ffa532c (fix commits 611a49b, 87fae4e; merge ffa532c brings main 95810f4), base 07c59e5, core 3bc36dab.

## Environment

Trees built in the session scratchpad (`.../scratchpad/k25r2`) from codeload tarballs: core `3bc36dab`, knowledge `ffa532c5` copied as a real directory to `core/addons/knowledge`, knowledge `07c59e5e` and a pristine `ffa532c5` alongside for comparison. `./setup-addons.sh`, `pnpm install --dir frontend --frozen-lockfile`, `node frontend/scripts/merge-addon-messages.mjs`. Baseline: `vitest run src/addons/knowledge/` 36 files / 830 tests pass; `tsc --noEmit` "No errors found", exit 0.

Mutation procedure: a script copies the target file, applies one exact-string replace (asserting the needle occurs once), checks the file differs from the copy, runs the whole `src/addons/knowledge/` suite, copies the original back and checks byte equality (`changed`/`restored` recorded True for every row). Reproductions that needed a harness used a scratch test file `src/addons/knowledge/__tests__/zz_k25_scratch.test.tsx` in the generated link tree, reusing the header of `NewNoteMenuItem.test.tsx`; removed at the end.

## Findings

### F1. Reporting the dialog closed before the create resolves survives the suite; inside the real Add menu a failed create then loses the dialog on the next press or Escape
- Invariant: 3 (on failure the dialog stays open and the user can submit again), 7 (pressing inside the dialog does not close it; first Escape closes only the dialog)
- `frontend/NewNoteMenuItem.tsx:60-71` (`onDialogOpenChange?.(false)` placed after `await createTextFile`); tests `frontend/__tests__/NewNoteMenuItem.test.tsx:210-234` (failure and retry, rendered with `renderRow`, no host menu) and `:303-325` (asserts `[[true],[false]]` only after a success)
- Mutation N17: move `onDialogOpenChange?.(false);` above `const file = await createTextFile(...)`. All 830 tests pass.
- Reproduction against the real core `AddButton` (scratch test, `addonProps={{ drive: "d", path: "notes" }}`, `createTextFile` rejects once):
  - R1: Save -> "already exists" shown -> `pointerDown` in the filename field -> dialog still present -> Save again -> navigates once, 2 creates. Pristine: pass. N17: fail (the press reaches the re-armed `DismissScrim`, the menu closes and unmounts the row with its portalled dialog).
  - R2: same failure -> Escape in the filename field -> dialog gone, menu still present. Pristine: pass. N17: fail (the Add menu's Escape is re-enabled, so the menu goes too).
  - R5: create pending -> `pointerDown` in the filename field -> dialog still present. Pristine: pass. N17: fail.
- Observed: live in the suite. Expected: kill — every failure/retry test runs without the host, where `onDialogOpenChange` has no effect, and the one host-backed group (`:327-370`) never fails a create.
- [introduced] (file created by the change)

## Scope 1: the removal is complete and neutral

`diff -rq` of the whole addon tree, `07c59e5e` vs `ffa532c5`: only `README.md`, `manifest.json`, `frontend/slots.ts`, `frontend/messages/{en,ja}.json`, `frontend/__tests__/{manifest,popup-dismissal,reachability}.test.ts` differ, plus the added `frontend/NewNoteMenuItem.tsx` and `frontend/__tests__/NewNoteMenuItem.test.tsx`. `KnowledgeDashboard.tsx` and every other file are byte-identical to the base (no `ClipForm.tsx`, `ClipWebPageDialog.tsx`, `ClipWebPageMenuItem.tsx`, `AddMenuRows.test.tsx`).

Separating the merge: `compare/07c59e5e...95810f4` (addon main) accounts exactly for `manifest.json` `navigation`, `knowledge.nav.label` in both locales, and the two navigation tests plus the widened type in `manifest.test.ts`. What remains for this change is: one `folder-actions-menu` entry, one `slots.ts` line, `knowledge.newNote.{menuItem,dialogTitle}` in both locales (both used by `NewNoteMenuItem.tsx`), the README "Add menu" section, `toHaveLength(7→8)`, the id→component row, the placement test, `FILE_COUNT 36→37`, `moduleFiles.size 36→37`. No clip message keys, no clip helpers remain.

Detector counts are not coincidental: both walkers skip `__tests__` and `*.test.tsx`, so 37 is the base's 36 plus `NewNoteMenuItem.tsx`; the `.ts` count stays 11. Adding an unreferenced `frontend/ZzDead.tsx` (relinked with `setup-addons.sh`) fails `popup-dismissal` "looks at the whole tree" and both `reachability` tests (want=kill, got=kill); removed and relinked afterwards. Manifest `toHaveLength(8)`: base 7 + `knowledge-new-note`; `navigation` is not a slot.

`NewNoteMenuItem.tsx` is not touched by `611a49b` or `87fae4e` (file lists read with `gh api .../commits/<sha>`), so the New note implementation is the one round 1 reviewed.

Known and planned, not a finding: in the folder toolbar the Add menu shows core's "New Note" (`onCreateFile`) and this "New note" together until P2-7 removes the core row (stage-2 brief S2-9/S2-13). The suite's host-backed tests pass no `onCreateFile`.

## Scope 3: do the tests in `87fae4e` hold what they name

- `manifest.test.ts` "puts New note in the Add menu": M1 (`folder-actions-menu` → `folder-actions-menux`) is killed by this test alone. Holds (closes r1 F4's New-note half).
- `NewNoteMenuItem.test.tsx` "New note created after its row was hidden / closes the menu and reports the dialog closed": N8 (drop `onDialogOpenChange(false)` on success) and N24 (skip close/report when the policy has turned off) are killed by this test alone; N9 (drop `onRequestClose`) also killed by it. The precondition is reached (it waits for the row to be gone before Save). Holds. It does not cover the failure path — see F1.
- The negative catalogue test (`:159-167`) is not vacuous: the same wait with a catalogue that carries knowledge draws the row (scratch R4 fails its `not.toBeInTheDocument()`), in isolation and in sequence.

## Round 1 claim re-measured: focus on open

`FileSaveDialog` does move focus into the dialog on open: its `[open]` effect calls `selectStem(el)`, which runs `el.focus()` (`frontend/src/lib/filename.ts:39-44`); opened from the real `AddButton`, `document.activeElement` is the Filename field (scratch R3 passes; deleting `selectStem(el)` fails it) — round 1's "FileSaveDialog has none either" was wrong.

## Trajectory question

Read in order: `611a49b` deletes `ClipForm.tsx`, `ClipWebPageDialog.tsx`, `ClipWebPageMenuItem.tsx`, the clip test file, the clip slot/manifest/message entries and reverts `KnowledgeDashboard.tsx` to the base — it removes the stage machine, the WebSocket wait, `formKey` and the duplicate branch; it adds no branch, state or prediction to implementation code (the replacement test file holds the New note behaviour that already existed). `87fae4e` adds two tests and no implementation. No round adds a branch, a state or a prediction that the round before also added; the only implementation diff since round 1 is a removal. On this record the loop is converging, not being patched. (F1 is a test gap on code unchanged since round 1, not the product of a fix.)

## Mutation table

Suite: `vitest run src/addons/knowledge/` unless noted (`scratch` = the host-backed scratch file). All rows `changed=True restored=True`.

| id | target | want | got | reading |
|---|---|---|---|---|
| N1 | rowHidden drop `!policy.isLoading &&` | live | live | equivalent: `usePolicy` is enabled=true while loading |
| N2 | hide while loading | kill | kill | |
| N3 | row ignores rowHidden | kill | kill | |
| N4 | dialog also gated on rowHidden | kill | kill | |
| N5 | cancel: drop `onDialogOpenChange(false)` | kill | kill | |
| N6 | cancel: drop `setDefaultFilename(null)` | kill | kill | |
| N7 | open: drop `onDialogOpenChange(true)` | kill | kill | |
| N8 | success: drop `onDialogOpenChange(false)` | kill | kill | killed only by the 87fae4e test |
| N9 | success: drop `onRequestClose` | kill | kill | |
| N10 | navigate before create | kill | kill | |
| N11 | join always `${folder}/${filename}` | kill | kill | |
| N12 | `defaultFolder=""` | kill | kill | |
| N13 | `path` default `"remembered"` | live | live | inert: every core caller passes `path` (`FolderToolbar`, `DriveHome`) |
| N14 | `defaultFolder` falls back to `knowledge:lastSubfolder` | kill | kill | |
| N15 | `surface` present resets path | kill | kill | |
| N16 | success: drop `setDefaultFilename(null)` | live | live | inert: the host unmounts the row on `onRequestClose` |
| N17 | report close before `await createTextFile` | kill | **live** | F1; scratch R1/R2/R5 kill it |
| N18 | `onRequestClose` before create | kill | kill | |
| N19 | swallow create error | kill | kill | |
| N20 | policy feature `index` | kill | kill | |
| N21 | policy addon `knowledgex` | kill | kill | |
| N22 | hide once policy answers | kill | kill | |
| N23 | effect reports close when policy turns off | kill | kill | |
| N24 | skip close/report when policy off at success | kill | kill | killed only by the 87fae4e test |
| N25 | portal to `document.body` instead of host | live | live | inert here: only `MobileInspectorSheet` provides a portal target, and no Add menu is inside it |
| N26 | `router.replace` | live | kill | test asserts `push`; my expectation was wrong |
| N27 | navigate without `?edit=1` | kill | kill | |
| M1 | manifest slot id typo | kill | kill | killed only by the 87fae4e test |
| M2 | slots.ts new-note → CreateNoteMenuItem | kill | kill | |
| M3 | drop `knowledge-create-note` entry (inv. 8) | kill | kill | |
| M4 | en `menuItem` → "Create note" | kill | live, then kill | first run unapplied (merged `src/messages` is generated); re-run after `merge-addon-messages.mjs`: 16 fail; restored and re-merged |
| D1 | add unreferenced `frontend/ZzDead.tsx` | kill | kill | popup-dismissal + reachability |
| C1 | core AddButton `onDialogOpenChange` no-op | kill | kill | |
| C2 | core AddButton Escape enabled while dialog open | kill | live | the dialog's Escape registers later at the same priority and answers first; core, not this change |
| C3 | core scrim `disabled={false}` | kill | kill | |
| C4 | core closeMenu no trigger focus | kill | kill | |
| C5 | core FileSaveDialog Escape off | kill | kill | |
| C6 | core FileSaveDialog no `setSubmitting(true)` | kill | kill | |
| C7 | core FileSaveDialog failure keeps submitting | kill | kill | |
| N17s | N17 | kill (scratch) | kill | R1, R2, R5 fail |
| F0s | core FileSaveDialog drop `selectStem(el)` | kill (scratch) | kill | R3 fails |

Final state: addon tree `diff -rq` against pristine `ffa532c5` empty; `frontend/src/components` `diff -rq` against the `3bc36dab` tarball empty; scratch test removed; suite 830/830.

TOTAL: 1 findings
