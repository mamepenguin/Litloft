# R1 findings: 02afe4757 (inline arrow page turn), parent c27bd52a6

Worktree: /Users/libre/Sources/video_share-pageturn-review. Tree restored after every mutation (`git status` shows only the symlinked layout).
Baseline: vitest (PdfPreview, epub/, epubReaderCore, hooks/) 43 files / 579 tests pass; `playwright-epub` 83 pass / 3 skipped.
Harness: scratchpad/pageturn-review/mut/run.py (exact before/after strings per mutation).

## Mutation table

| id | target | mutation | want | result |
|---|---|---|---|---|
| M1 | PdfPreview arrows | drop `arrowright` entry | kill | KILLED (arrow nav test, resume "turned to with the arrow") |
| M2 | PdfPreview arrows | swap → / ← handlers | kill | KILLED |
| M3 | PdfPreview save path | `arrowright` → `setPage(p+1)` (bypasses turnTo/pageTurned) | kill | KILLED (resume) |
| M4 | PdfPreview enable | drop `&& inScope` | kill | KILLED (scope tests) |
| M5 | PdfPreview enable | `numPages > 1` → `> 0` | live | LIVE (1-page doc: clamp makes it a no-op; equivalent) |
| M6 | movePage clamp | drop lower `Math.max(1, …)` | kill | KILLED |
| M6b | movePage clamp | drop upper `Math.min(numPages, …)` | kill | **LIVE** → F1 |
| M7 | useFocusScope | `!active ||` → `false ||` | live | LIVE (activeElement is never null with a body; equivalent) |
| M8 | useFocusScope | drop `active === document.body` | kill | KILLED (PDF + EPUB) |
| M9 | useFocusScope | `contains(active)` → `false` | kill | KILLED (PDF "takes the key back") |
| M10 | useFocusScope | drop focusin listener | kill | KILLED (PDF + EPUB) |
| M11 | useFocusScope | drop focusout listener | kill | **LIVE** → F2 |
| M12 | useFocusScope | drop initial `update()` | kill | **LIVE** → F3 |
| M12b | useFocusScope | `useState(true)` → `false` | live | LIVE (initial `update()` corrects it in the same commit; equivalent) |
| M13 | EpubPreview arrows enable | drop `&& inScope` | kill | KILLED |
| M14 | EpubPreview arrows enable | drop `!fullscreen.isFullscreen` | live | LIVE (fullscreen context is OVERLAY_PRIORITY + blocksLower; equivalent) |
| M15 | EpubPreview arrows enable | drop `ready &&` | live | LIVE (pre-ready turn is posted to a reader that has no view; no file nav on EPUB anyway) |
| M16 | EpubPreview direction | swap left/right | kill | KILLED |
| M17 | EpubPreview direction | left/right → prev/next | kill | KILLED (rtl fixture) |
| M18 | EpubPreview arrows | context disabled | kill | KILLED |
| M19 | core.js keyAction | restore `fullscreen &&` gate on arrows | kill | KILLED (unit + playwright chromium/webkit) |
| M20 | core.js keyAction | swap left/right | kill | KILLED (unit + playwright) |
| M21 | core.js keyAction | inline arrows → prev/next | kill | KILLED (unit) |
| M22 | core.js FORWARDED | add `ArrowLeft` to inline forward set | live | LIVE (arrow branch precedes the forward check; unreachable) |

Invariants 1-8: no path found that breaks them in the code as written. The three survivors below are tests that would let a break of 1 / 2 / 4 through.

Is anything missing from the R-0 list? One candidate: *"An arrow with a modifier is not a page turn: Shift/Alt/Ctrl/Meta+arrow behave as they did before this change, on the page and inside the book."* See F4 — the two EPUB paths currently disagree on Shift.

## Findings

### F1. Upper clamp of `movePage` is untested — `[pre-existing]`, bucket B (A if the supervisor treats a test gap on invariant 1 as A)
- `frontend/src/components/PdfPreview.tsx:348-353` (`movePage`); `turnTo` (`:153-163`) does not clamp.
- Reproduction: M6b (`Math.min(numPages || 1, Math.max(1, …))` → `Math.max(1, …)`). HEAD: 83/83 pass. Same mutation on parent c27bd52a6 with its own tests: 80/80 pass.
- Observed: → on the last page would set `page = numPages + 1` and record it through `pageTurned`, with no test failing. The code is correct today; the arrow (new) and PageDown (old) both reach the unguarded path. Invariant 1 says "clamped to 1..numPages".

### F2. Returning to body from an outside focusable is untested (focusout listener) — `[introduced]` for EPUB (new file, new consumer), `[pre-existing]` for PDF; bucket A (test lets a break of invariant 2 through)
- `frontend/src/hooks/useFocusScope.ts:24` (`focusout` listener).
- Reproduction: M11. HEAD suite: all pass. Ad-hoc test (render hook, focus an outside button, `blur()`, read scope): passes at HEAD, fails under M11 (`false` stays). On parent, removing the same line from the inline PdfPreview code: 80/80 pass.
- Observed: the realistic path is "click the inspector tab strip, then click blank page area" — only `focusout` fires (body gets no `focusin`), so without that listener the EPUB and PDF arrows stay dead until something inside the viewer is focused. Nothing in either suite exercises focus returning to body.

### F3. Mounting while an outside element holds focus is untested (initial `update()`) — `[introduced]` for EPUB, `[pre-existing]` for PDF; bucket B
- `frontend/src/hooks/useFocusScope.ts:22` (initial `update()`).
- Reproduction: M12. HEAD suite: all pass. Ad-hoc test (focus an outside button, then mount): `false` at HEAD, `true` under M12. Parent with the line removed: 80/80 pass.
- Observed: under M12 the viewer claims the arrows on mount although focus sits outside it (e.g. SPA navigation to a PDF/EPUB leaves focus on a button outside the viewer), so ← / → would turn a page while the focused element also handles them — invariant 4. Reachability depends on whether any outside focusable survives the navigation; not measured in a browser.

### F4. Shift+arrow inside the book now turns the page inline; on the page it does not — `[introduced]`, bucket B
- `frontend/public/epub-reader/core.js:29-30` (arrow branches ignore `shift`); `reader.js:95` returns only for meta/ctrl/alt.
- Reproduction: `keyAction("ArrowRight", { shift: true, fullscreen: false })` → HEAD `{ turn: "right" }` (and `preventDefault`), parent `null`. On the page side `normalizeKey` yields `shift+arrowright`, which matches no context, so the inline shortcut does not fire.
- Observed: with focus in the book, Shift+→ (extending a text selection by keyboard) turns the page instead; with focus on the page the same chord does nothing. Full screen already behaved this way before the change. Not an R-0 break; raised as the candidate invariant above.

TOTAL: 4 findings
