# R1 review: 7aa837391 (stop arrow-key file navigation on PDF and EPUB)

Reviewed at the fixed SHA in `/Users/libre/Sources/video_share-arrownav-review`.

Environment note: the worktree's `frontend/node_modules`, `frontend/src/addons` and
`frontend/src/messages` symlinks were relative links that resolved inside the review
worktree itself (broken; `vitest` not found). I re-pointed those three untracked links
to absolute paths under `/Users/libre/Sources/video_share-arrownav/frontend/`. No tracked
file was left modified (`git status` shows only the pre-existing `addons/*` gitlink
deletions and the untracked links).

Baseline at the SHA: the 10 targeted vitest files 321/321 pass; EPUB Playwright (chromium +
webkit) 83 passed, 3 skipped. Full vitest: 7612 pass, 1 fail
(`addon-link-tree.test.ts`, also fails unmutated -- caused by the symlinked review layout,
unrelated).

Unit set used for every vitest mutation: useFileNav, epubReaderChannel, epubReaderCore,
components/epub, RightPaneFile, TwoPaneLayout, FileNavControls, PdfPreview,
FileDetailFullScreen, FilePreview tests.

## Mutation table

| # | Target | Mutation | want | result |
|---|---|---|---|---|
| M1 | useFileNav.ts shortcutsEnabled | drop `&& !PAGED_MIMES.has(...)` | kill | killed (useFileNav PDF + EPUB tests) |
| M2 | PAGED_MIMES | remove `application/pdf` | kill | killed (PDF test) |
| M3 | PAGED_MIMES | remove `application/epub+zip` | kill | killed (EPUB test) |
| M4 | shortcutsEnabled | `!PAGED_MIMES.has` -> `PAGED_MIMES.has` | kill | killed (6 tests) |
| M5 | shortcutsEnabled | replace PAGED check with `fileType !== "document"` (over-broad, breaks inv 3 for text/md) | kill | killed (ArrowLeft/ArrowRight document tests) |
| M6 | RightPaneFile.tsx | pass `mimeType: null` to useFileNav | kill | **LIVE** (321/321; also full suite: nothing new fails) -> finding 1 |
| M7 | FileDetailFullScreen.tsx | pass `mimeType: null` to useFileNav | kill | **LIVE** (321/321; full suite likewise) -> finding 1 |
| M8 | core.js FORWARDED.inline | re-add ArrowLeft/ArrowRight | kill | killed (epubReaderCore x2; Playwright reader.spec 2 failed, chromium+webkit) |
| M9 | core.js keyAction | drop `fullscreen &&` on ArrowLeft (unit) / both arrows (e2e) so arrows turn inline | kill | killed (epubReaderCore; Playwright 2 failed) |
| M10 | epubReaderChannel.ts KEYS | add `ArrowLeft` | kill | killed (channel "drops" case + EpubPreview "does not move to another file") |
| M11 | core.js FORWARDED.inline | remove `f` | kill | killed (epubReaderCore) |
| M12 | core.js FORWARDED.fullscreen | remove `Escape` | kill | killed (epubReaderCore) |
| M13 | core.js keyAction | delete fullscreen ArrowRight turn | kill | killed (epubReaderCore); an unrelated PdfPreview test also timed out once -> finding 3 |
| M14 | useFileNav navigatePrev/Next | gate on `!PAGED_MIMES.has(mimeType)` (buttons dead on PDF/EPUB) | kill | **LIVE** (321/321) -> finding 2 |
| M15 | useFileNav return | report prevId/nextId null for paged mimes (buttons disabled) | kill | killed (indirectly: the new tests' `waitFor(prevId)` times out) |
| M16 | EpubPreview.tsx fullscreen scope | remove arrowleft entry | kill | killed (EpubPreview "in full screen the arrows turn pages") |
| M17 | PdfFullscreenViewer.tsx | rename arrowright key | kill | killed (3 PdfPreview tests) |
| P1 | probe, not a mutation: reader.spec inline-arrow test with 5 extra ArrowRight presses and a 2 s wait before comparing `where()` | want=pass (native default of an unhandled arrow must not scroll the paginator) | passed, chromium + webkit -> invariant 2 holds in real browsers; the unhandled (no preventDefault) arrow does not move the book |

Also checked by reading: FilePreview picks PdfPreview / EpubPreview on exactly
`application/pdf` / `application/epub+zip`, and backend `filetype.py` emits exactly those
mimes for `.pdf` / `.epub`, so PAGED_MIMES matches every file that reaches either reader.
File seeds (`peekFileSeed`) are full FileItems, so mimeType is present from the first
render in both hosts. No other `arrowleft/arrowright` binding on the file detail page
(collection queue has none). No remaining prose claims the arrows change file on PDF/EPUB.

## Findings

### 1. The mimeType wiring from both hosts into useFileNav is untested; invariant 1 now depends on it
- Label: [pre-existing] test gap (RightPaneFile.test.tsx / FileDetailFullScreen.test.tsx and both call sites are unchanged by this commit, so M6/M7 survive on 9f3557f6a too, where the only consequence was .loft). This commit makes it load-bearing for invariant 1.
- Bucket suggestion: A (a test that lets an invariant-1 violation through) -- but the label is pre-existing, so per review-workflow Scope this is the supervisor's call.
- Location: `frontend/src/components/folder/RightPaneFile.tsx:79`, `frontend/src/components/FileDetailFullScreen.tsx:90`.
- Reproduction: M6 / M7 -- replace `mimeType: file?.mime_type ?? null` with `mimeType: null`; run the unit set and the full vitest suite.
- Observed: all green. With either mutation, ← / → on a PDF or EPUB page in that host go to the previous / next file again (useFileNav's new tests cover the hook only; both host tests mock useFileNav and never assert the `mimeType` it receives). RightPaneFile.test.tsx already inspects `useFileNavMock.mock.calls.at(-1)[0]` for sort/order, so the assertion has an obvious home.

### 2. No test holds that the previous / next buttons still change file on PDF / EPUB
- Label: [introduced] (PAGED_MIMES and the invariant-5 promise for paged files are new in this commit).
- Bucket suggestion: A (test gap for invariant 5; the code itself is correct today).
- Location: `frontend/src/hooks/useFileNav.ts:83-89` (navigatePrev / navigateNext), tests in `frontend/src/hooks/__tests__/useFileNav.test.tsx:188-207`.
- Reproduction: M14 -- make navigatePrev/navigateNext return early when `PAGED_MIMES.has(mimeType ?? "")`.
- Observed: 321/321 pass. The new "does not bind arrow keys for PDF/EPUB" test only asserts the keys are inert; nothing calls `result.current.navigatePrev()/navigateNext()` for a paged mime and expects onNavigate. The existing "hands out the same navigation the arrow keys run" test uses a document without a paged mime.

### 3. One-off timeout in an unrelated PdfPreview test under the parallel run
- Label: [pre-existing].
- Bucket suggestion: B.
- Location: `frontend/src/components/__tests__/PdfPreview.test.tsx` "PdfPreview full screen > follows a link inline again after the full-screen viewer has closed".
- Reproduction: appeared once (3010 ms timeout) during M13, a mutation to `public/epub-reader/core.js`, which PdfPreview does not import. Re-ran PdfPreview alone 5x and the 10-file set 4x unmutated: all green.
- Observed: flaky near its 3 s bound under load; not caused by this change.

## Invariant list: anything missing?
One suggestion: inline, `f` typed inside the EPUB iframe must still be forwarded and open full screen. Invariant 6 names `f` / `Esc` only for full screen, but this commit edited `FORWARDED.inline` (the set that carries inline `f`), so it is a path the change touches. (It is held today: M11 is killed by epubReaderCore, and the edited reader.spec test asserts the `f` message.)

TOTAL: 3 findings
