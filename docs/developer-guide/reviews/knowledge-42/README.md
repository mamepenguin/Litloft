# knowledge #42 — New note writes to the folder chosen in its dialog

A test-only pull request in the knowledge addon repository, holding what
PR #268's round 1 found unheld in F2 (`../268/r1.md`). It changed no behaviour,
so it had no review of its own.
