# page-frame — review round 2

Reviewed: fix commit `e535760b` (parent `df4f5734`), with `353bdf4a` (test-only) as context. Working tree at `6c030a86`, clean before starting.

Baseline at HEAD: the 5 touched unit files pass (127 tests); `CI=1 pnpm test:e2e:components page-frame` passes (8); `npx tsc --noEmit -p .` clean.

## Mutation table

| # | File | Mutation | want | result |
|---|---|---|---|---|
| N1 | PageFrame.tsx | drop `flex-1` from the fixed class list | kill | **live** (unit, e2e, tsc). No observable effect found: see "flex-1 on the frame" below |
| N2 | PageFrame.tsx | drop `w-full` | kill | killed e2e (3) — F3's fixture change now holds it |
| N3 | PageFrame.tsx | drop `min-w-0` | live | live (expected: every parent is a column flex container, where the automatic minimum is on the height, not the width) |
| N4 | PageFrame.tsx | `wide` -> `max-w-list-row` | kill | killed unit (1), e2e (1) |
| N5 | PageFrame.tsx | add `pt-4` to the fixed class list | kill | killed unit (1), e2e (8) |
| N6 | TrashView.tsx | body `px-4 pb-4 sm:pb-6` -> `px-4` | live | live (no invariant covers bottom padding) |
| N7 | MissingView.tsx | body `px-4 pb-4 sm:pb-6` -> `px-4` | live | live (same) |
| N8 | CollectionDetail.tsx | body `px-4 pb-6` -> `px-4` | live | live (same) |
| N9 | TrashView.tsx | wrap the returned `PageFrame` in `<div className="pt-4">` | kill | **live** (unit 127/127, e2e 8/8, tsc 0) — F1 below |
| N10 | TrashView.tsx | pass `className="pt-4"` to `PageFrame` (round 1's M17 shape) | kill | killed by tsc (1 error; CI runs `pnpm exec tsc --noEmit`). Unit and e2e alone would pass |
| N11 | TrashView.tsx | body gains `pt-4` | live | live (expected: moves the toolbar, not the title; R-0 #1 is about the h1) |
| N12 | CollectionDetail.tsx | `width="wide"` -> `"full"` | kill | killed unit (1) |

Tree restored after every mutation (`git checkout -- <file>`).

### Probes (not mutations of the commit)

- **Collection in `TwoPaneLayout`'s section** (`flex h-full flex-col overflow-y-auto`). Temporary fixture arrangement + spec in `e2e-components/` (Chromium, 1425px, section 400px tall), comparing the frame as it is at `df4f5734` (no `flex-1`, `pb-6` on the frame) with `e535760b` (`flex-1` on the frame, `pb-6` on the body):

  | case | scrollHeight | frame height | frame width | gap below last content, scrolled to end | h1 top |
  |---|---|---|---|---|---|
  | df4f5734, tall body | 1098 | 1097.6 | 1152 | 24.4 | 8 |
  | e535760b, tall body | 1098 | 1097.6 | 1152 | 24.4 | 8 |
  | df4f5734, short body | 400 | 147.6 | 1152 | 276.4 | 8 |
  | e535760b, short body | 400 | 400 | 1152 | 276.4 | 8 |

  `flex-1` (basis 0) inside the scroll container does not squeeze the frame below its content (the item's automatic min-height holds), the scroll length and the 24px under the last row are unchanged, and the capped width still resolves to 1152. The only difference is the frame's own box filling the section when the content is short; it has no background or handlers, so nothing shows. Probe files removed; fixture restored.
- **Trash / Missing** are standalone views (`STANDALONE_VIEWS`), rendered straight into `AppShell`'s `<main className="flex min-w-0 flex-1 flex-col">`; they already carried `flex-1` at `df4f5734`. The infinite-scroll sentinel was inside the `px-4` body before and is still inside it, above the moved padding; the only siblings after the body are `SelectionBar` and `ConfirmDialog`, both `position: fixed`, so moving the padding from the frame to the body changes neither the scroll length nor where the sentinel sits relative to the end. Not measured in a browser (same box arithmetic as the Collection probe, without the scroll container).
- **DriveHome / FolderBrowser** lost `className="flex-1"` and gained the frame's own `flex-1`: the rendered class set is identical.

### flex-1 on the frame

N1 is live, and the assertion `frame.classList.contains("flex-1")` was deleted from `PageFrame.test.tsx` by this commit. That deleted assertion never held the frame at a real screen — at `df4f5734` the test passed `className="flex-1"` itself, and removing `className="flex-1"` from `DriveHome.tsx` there also survived (reproduced: 34/34). Nothing inside any of the five frames sizes itself against the frame's height (every body is a block `px-4` div, no `flex-1` child), so no observable consequence of dropping it was found. Not raised as a finding.

## Findings

### F1 — a page can still put space above its title, by wrapping the frame
- Label: `[pre-existing]` relative to `e535760b` — the same wrapper mutation on `df4f5734`'s `TrashView.tsx` passes the five unit files (127/127) in a worktree at that SHA. What `e535760b` closed is the `className` route only (N10, killed by tsc).
- R-0: #1 (title Y is the header's `py-2` and nothing more) — not broken by the code; not held by any test on a real screen.
- Where: every caller returns `<PageFrame …>` as its root (or, in FolderBrowser, inside `UploadZone`); nothing checks that the frame's top is the top of the screen's column. `page-frame.spec.ts` measures `h1Top` relative to the frame (`frameEl.getBoundingClientRect().top`) on a fixture, and the per-screen unit tests assert `data-page-frame` only.
- Reproduction: N9 — `return (<div className="pt-4"><PageFrame …>…</PageFrame></div>)` in `TrashView.tsx` survives unit, e2e and tsc. The same shape applies to the other four screens (and to the search page's existing `px-4 pt-3 pb-1` row above `FolderBrowser`, which is outside this phase's screens).
- Does `e535760b` do what it claims? Its message claims the frame takes no class and fills its column: yes (N5, N10). Round 1's F1 was triaged A as "a test that lets an R-0 #1 break through"; the commit removes the route round 1 demonstrated, but the test gap F1 described — nothing measures the title's Y on the screens themselves — remains, reachable through the next-nearest edit.
- Severity: low–medium. Suggested bucket: supervisor decides whether F1's A covered the test gap or the `className` route; if the former, this is still A; if the latter, B.

### Already triaged (not re-derived)
- Round 1 F2 (children inset tested on Home only): `[pre-existing]`, triaged B. `e535760b` edits the Trash, Missing and Collection body class strings but keeps `px-4` as their only inset; still untested there.
- Round 1 F4 (failed listing shows `drive · 0 items`): `[pre-existing]`, triaged B; untouched by `e535760b`.
- Round 1 F5 (Library root has no link to the drive picker): triaged B; untouched by `e535760b`.

## Trajectory question

Read in order: `e535760b` removes a prop (`className`) from `PageFrame` and the four call-site strings that used it; the frame's own class list becomes a constant (it gains `flex-1`, which every call site but Collection already passed). That removes a state — caller-supplied variation of the frame — and adds no branch. `353bdf4a` adds no code: it changes the fixture's parent to a flex column so the existing measurement runs in the shape the screens use, with no new expectation in the spec. Neither commit adds a branch, a state or a prediction that the other also added; this round removes one. The loop is converging, not patching. (Only one fix round exists, so the "two rounds in a row" test cannot trigger yet.)

## R-0 items against `e535760b`

1. Held by the code; fixture measurement still kills top padding on the frame (N5). Not held on the screens themselves (F1).
2. Unchanged: each body still `px-4`, header's own `px-4`; widths unchanged (N4, N12 killed).
3. Not touched by this commit (breadcrumb/drop code unchanged).
4. Not touched.
5. Not touched — no accent-filled control added or moved.
6. Not touched; the fixture now runs in a flex column and still passes at 375px.

`git status` at the end: clean apart from this file.

TOTAL: 1 findings
