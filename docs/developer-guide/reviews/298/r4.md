# page-frame — review round 4

Reviewed: `3a4046a7` and `2b2ebaa7` together (parent `d6842a50`), the response to round 3. Tree clean before starting.

Baseline at `2b2ebaa7`: `pnpm test` 491 files / 7121 tests pass (2 todo); `npx tsc --noEmit -p .` clean; `CI=1 pnpm test:e2e:components` **89 pass, 1 fail** (see F1).

## Mutation table

Unit runs are `FixedTreeToggle`, `MenuButton`, `TreeToggle`, `sidebar/__tests__/*`, `SidebarAddonNav`, `driveHomeTreeClosable`, `DriveHome`, `Header`, `Header.quickNote`, `sidebarShellFixtureParity`, `AppShell.globalSearch`, `AppShell.quickNote`, `FolderBrowser.header` (33 files); rows marked "full" were re-run on the whole `pnpm test`. Each mutation was applied by a script and the file restored with `git checkout -- <file>` afterwards.

| # | File | Mutation | want | result |
|---|---|---|---|---|
| M1 | AppShell.tsx | drop `<FixedTreeToggle />` | kill | killed (3, MenuButton) |
| M2 | FixedTreeToggle.tsx | `left-[60px]` -> `left-3` | kill | killed (4) |
| M3 | FixedTreeToggle.tsx | `z-50` -> `z-30` | kill | killed (1, class assertion) |
| M4 | FixedTreeToggle.tsx | top `+ 12px` -> `+ 0px` | kill | killed (1) |
| M5 | FixedTreeToggle.tsx | `fixed` -> `absolute` | kill | killed (1) |
| M6 | FixedTreeToggle.tsx | drop `if (!drive) return null` | live (`TreeToggle` has its own `/drive/` guard) | "killed" (1) for an incidental reason: `AppShell.quickNote`'s `next/navigation` mock has no `useSearchParams`, which `TreeToggle` now reaches. No behaviour assertion is involved |
| M7 | TreeToggle.tsx | `?file=` display always `flex` | kill | killed (2) — r3 F2 held |
| M8 | TreeToggle.tsx | `?file=` condition inverted | kill | killed (3) |
| M9 | TreeToggle.tsx | `return null` whenever `?file=` is present | kill | killed (2) — r3 F4 held |
| M10 | AppShell.tsx (MenuButton) | `left-3` -> `left-12` | kill | **live (full)** — F3 |
| M11 | AppShell.tsx (MenuButton) | top `+ 12px` -> `+ 20px` | kill | **live** — F3 |
| M12 | AppShell.tsx (MenuButton) | `h-10 w-10` -> `h-12 w-12` | kill | **live** — F3 |
| M13 | SidebarLibrarySection.tsx | drop `shrink-0` from the button row | kill | killed (1, class assertion; the collapse itself measured below) |
| M14 | SidebarLibrarySection.tsx | row `h-10` -> `h-6` | kill | killed (1) |
| M15 | SidebarLibrarySection.tsx | delete the row | kill | killed (1) |
| M16 | Header.tsx | render `{drive && <TreeToggle drive={drive} />}` before the spacer again | kill | **live (full)** — F4 |
| M17 | DriveHome.tsx | `leading={<TreeToggle drive={driveName} />}` on the page header | kill | killed (1, `DriveHome.test`) — r3 F3 held |
| M18 | TreeToggle.tsx | `onClick={() => {}}` | kill | killed (4, incl. `driveHomeTreeClosable`) — R-0 #8 held |
| M19 | AppShell.tsx | drop `<FixedTreeToggle />` and its import, run only `driveHomeTreeClosable` + `FixedTreeToggle` | live | live (those render `FixedTreeToggle` directly; M1 shows `MenuButton.test` is the one that holds the mount) |

## Browser probe

A static page in Chromium (Playwright 1.58, `setContent`), not the app: the menu button, the fixed toggle wrapper, the pre-`2b2ebaa7` header slot (`mt-3 self-start ml-11` inside `px-4`), the sidebar `aside`/`nav`, a sticky `z-20` toolbar holding a `fixed inset-0 z-30` scrim, all carrying the components' class lists. Rules come from `e2e-layout/fixtures/globals.built.css` (Tailwind v4, `--spacing: 0.25rem`); `left-[60px]` and `ml-11` are not in that sheet and were written by hand as `left: 60px` / `margin-left: calc(var(--spacing) * 11)`, which is what Tailwind emits for them. Root font size set on `<html>` to stand in for the browser's default-font-size setting. Boxes are `[left, right, top, bottom]` in px.

| layout | root | menu | toggle | overlap | topmost at menu's right edge | topmost at toggle centre, scrim open |
|---|---|---|---|---|---|---|
| parent (`d6842a50`, header slot) | 16 | 12,52,12,52 | 60,100,12,52 | 0 | menu | scrim |
| parent | 20 | 15,65,12,62 | 75,125,15,65 | 0 | menu | scrim |
| parent | 24 | 18,78,12,72 | 90,150,18,78 | 0 | menu | scrim |
| `2b2ebaa7` (fixed) | 16 | 12,52,12,52 | 60,100,12,52 | 0 | menu | toggle |
| `2b2ebaa7` | 20 | 15,65,12,62 | 60,110,12,62 | **5** | **toggle** | toggle |
| `2b2ebaa7` | 24 | 18,78,12,72 | 60,120,12,72 | **18** | **toggle** | toggle |

Sidebar `nav` (`flex h-full flex-col overflow-y-auto p-3`) with 60 rows so it overflows: with `shrink-0` the row is at top 12, height 40, next row at 64; without it the row is height 0 and the next row starts at 24.

Probe files are in the session scratchpad, not the repository.

## Findings

### F1 — the component e2e target is red: `page-frame.spec.ts` is assigned to no project

`[pre-existing]` (added on this branch by `df4f5734`; neither `3a4046a7` nor `2b2ebaa7` touches `frontend/e2e-components/`) · none · `frontend/e2e-components/spec-viewport.spec.ts:41-54` · medium (CI runs `pnpm test:e2e:components`, `.github/workflows/ci.yml:210`)

`CI=1 pnpm test:e2e:components` at `2b2ebaa7`: 89 pass, 1 fail — `spec-viewport.spec.ts › every spec in this directory is assigned to exactly one project`. The on-disk list contains `page-frame.spec.ts`; `AT_THE_PHONE_WIDTH` / `AT_THE_DESKTOP_WIDTH` do not. `page-frame.spec.ts` does not exist on `develop` (`git cat-file -e develop:…` fails), so the detector passes there. Earlier rounds ran the target filtered to `page-frame`, which skips this spec. Reproduced standalone with `npx playwright test --config=playwright-components.config.ts spec-viewport`.

### F2 — with a larger default font size, the fixed toggle covers the right edge of the menu button

`[introduced]` by `2b2ebaa7` · R-0 #7 ("fixed beside the menu button, in the menu button's box") · `frontend/src/components/FixedTreeToggle.tsx:17`, `frontend/src/components/AppShell.tsx:34` · low–medium (reachable by anyone who raised the browser's font size, e.g. Chrome "Font size: Large" = 20px; the menu button loses its right 5px of hit area at 20px and 18px at 24px)

The menu button's box is rem (`left-3` = 0.75rem, `w-10` = 2.5rem), the toggle's left is px (`left-[60px]`), and the toggle is later in the DOM at the same `z-50`, so it paints over the button and takes its clicks where they meet. Vertically the two still agree (both tops are `+ 12px`, both heights rem); only the horizontal offset fails to scale. At the parent the offset was `px-4` + `ml-11`, both rem, and the two never met.

Measured (probe above): root 16 → no overlap in either layout; root 20 → parent 0, `2b2ebaa7` 5px, topmost at the menu's right edge is the toggle; root 24 → parent 0, `2b2ebaa7` 18px. The probe is a static page with the components' classes, not the app.

### F3 — the toggle's position is a prediction of the menu button's box that no test ties to the menu button

`[introduced]` (the prediction lives in `FixedTreeToggle.tsx`, a file `2b2ebaa7` creates) · R-0 #7 · `frontend/src/components/__tests__/FixedTreeToggle.test.tsx` (last case), `frontend/src/components/__tests__/MenuButton.test.tsx` (new `it.each`) · low (test gap)

Both tests assert the toggle's own literal classes (`left-[60px]`, `h-10`, `w-10`, `rounded-2xl`, top containing `12px`). Nothing asserts the menu button's `left-3`, `w-10` or top. M10 (`left-3` -> `left-12`, which puts the menu at 48–88 under the toggle at 60–100), M11 (menu top +20px) and M12 (menu 48px) survive (M10 on the full suite, M11/M12 on the 33 related files). The comment in `FixedTreeToggle.test.tsx` states the menu's geometry as a fact the test depends on, but the test never reads it. jsdom could at least compare the two elements' class tokens and `style.top` strings in one render of `AppShell`; it cannot catch F2, which needs a real layout.

### F4 — "no app header draws one" is no longer held

`[introduced]` by `2b2ebaa7` · R-0 #7 ("No page header, no app header and no file-detail chrome draws one") · `frontend/src/components/Header.tsx:59`, `frontend/src/components/__tests__/FixedTreeToggle.test.tsx` (renamed from `Header.treeToggle.test.tsx`) · low (test gap)

M16 puts `{drive && <TreeToggle drive={drive} />}` back in `Header` before the spacer; the full suite stays green (491/491). The renamed test no longer renders `Header`; `Header.test` and `Header.quickNote.test` do not look for a tree control; `MenuButton.test` mocks `Header` to `<header />`; `driveHomeTreeClosable` renders `FixedTreeToggle` and the drive layout without `Header`, and its finder takes the first "Hide tree" button outside an `aside`, so a second one would not fail it. The page-header side is held for each screen (M17 and r3 M9/M10); the app-header side, which this commit is the one to empty, is not.

## Checked, no finding

- **r3 responses.** F2 (narrow + open file): `hidden md:flex` on `?file=`, held (M7, M8). `md` (768, min-width) and `useIsMobile` (`innerWidth < 768`) agree, so where the mobile inspector sheet (`z-[45]`/`z-[46]`, below 768 only) opens, the toggle is already hidden. `TwoPaneLayout`'s `hasFile` requires a non-empty id while the toggle hides on any `file` param, so a bare `?file=` would hide the toggle over a full tree — not reachable from the UI. F3: `DriveHome.test`'s new mock always renders a button and the new case asserts none; M17 kills it. F4: M9 kills it. F5: the header placement it was about no longer exists; the fixed wrapper's classes are asserted (M2–M5). F6/F7: the toggle no longer reads sidebar state (`FixedTreeToggle` has no `useSidebar`), so neither the 192px jump nor the transitional overlap can occur; confirmed by the removed branch and M2 killing any change of the literal left.
- **Stacking.** The toggle left the header's `z-20` stacking context for the root one at `z-50`. Measured in the probe: at the parent a `z-30` scrim inside a sticky `z-20` toolbar covered the toggle; now the toggle is topmost there, as the menu button already was. Behaviour does not change: `DismissScrim` dismisses on a document capture-phase `pointerdown` and swallows the resulting click whatever is under the pointer (`DismissScrim.tsx`), so pressing the toggle with a popup open only closes the popup (read, not run in the app). Below 640px the dimmed menu scrim now leaves two undimmed buttons at the top-left instead of one. Portaled or higher overlays stay above it by class: `GlobalSearch` (portal to `body`, `z-50`, later in the DOM), Quick Note (portal, `z-[100]`), `ImageGallery` / `ArchiveImageViewer` (`z-[60]`), `FileSaveDialog`, toasts, shortcut sheet (`z-[100]`); dialogs at `fixed z-50` rendered inside `main` come later in the DOM than the toggle. `ContextMenu`'s scrim is `z-49`, same as the menu button case. `VideoPlayer`'s pseudo-fullscreen (`fixed inset-0 z-50`, not portaled) sits wherever its ancestors' stacking puts it; whatever that is, the menu button was already in the same position relative to it. Derived from classes except the scrim row.
- **Open overlay sidebar.** Measured: the toggle is now topmost over the open sidebar's top row (at the parent the `aside` covered it). This is what R-0 #7 asks for. Pressing it there does not close the sidebar; at 768–1199 the pane opens behind the scrim, below 768 the full-width tree opens behind it. Both are visible through the scrim.
- **What sits under x=60–100, y=12–52.** The app header's left is now only the `flex-1` spacer; its right group is search, Quick Note, knowledge's `header-actions` entry (draws nothing, `ClipNotifier`) and the avatar, far from x=100 at 375px. Page content starts below the 56px header (`TwoPaneLayout` is `h-[calc(100dvh-3.5rem)]` under it; the folder toolbar's `sticky top-0` is inside the right pane's scroll box). On routes without a tree pane `FixedTreeToggle` renders an empty 0×0 fixed `div` (or nothing without a drive).
- **Sidebar row.** Measured: `h-10 shrink-0` keeps the row at 12–52, level with the buttons, when the sidebar overflows; without `shrink-0` it collapses to 0 and the drive switcher rides up under the buttons. The logo row was 44px + `mb-2` (`py-2` around a 28px `text-lg` line), so everything below moves up 4px. On `/`, `/admin`, `/settings` the row holds only the menu button and the sidebar still has a Home row linking to `/` (`driveBase ?? "/"`). Nothing else referenced the logo: no e2e, fixture or addon selects "Litloft" or the `Warehouse` icon in the sidebar; no code focuses the sidebar's first link.
- **Focus order.** DOM order is sidebar, menu button, toggle, header controls — the same sequence as at the parent, where the toggle was the header's first button.
- **References.** No remaining `data-app-header` in `frontend/`, `addons/` or `docs/` outside the r3 record. `--app-header-h` is unrelated and still published by `Header`.
- **Rewritten tests.** `FixedTreeToggle.test` renders the real `TreeToggle` (only the drive provider and `next/navigation` mocked), so its route tables are not vacuous (M7–M9). `MenuButton.test`'s three sidebar states cannot differ today, but they would catch a sidebar branch returning to the toggle's position (M1, M2). `driveHomeTreeClosable` clicks the real control and closes the real pane (M18). The sidebar label-list tests are exact lists, and `SidebarLibrarySection.test` asserts the absence of both the text and a link to `/` (M15 kills the row test; restoring the logo would fail the label lists). `popup-dismissal`'s file count 430 → 431 matches the one file added.

## Trajectory question

In order:

- `e535760b` removes an option (`PageFrame`'s `className`).
- `353bdf4a` is test-only.
- `60a4ce29` removes `FileDetailChrome`'s `showTreeToggle` branch, three `leading` placements and the cross-file height prediction, and adds a sidebar-state branch in `Header` (a two-valued prediction of the menu button's position) plus the route predicate in `TreeToggle`.
- `3a4046a7` adds a `?file=` display branch. It is the width gate `FileDetailChrome` wrapped its toggle in before `60a4ce29`, moved into `TreeToggle`: restored behaviour, but it is still a branch added to the file that gained one the round before.
- `2b2ebaa7` removes the `Header` sidebar-state branch, the `useSidebar` dependency and the header slot. It keeps a prediction of the menu button's geometry, reduced from two values to one (`left-[60px]`).

Read strictly, `60a4ce29` → `3a4046a7` is an add followed by an add. But the second add restores a gate that existed before, and the pair `3a4046a7` + `2b2ebaa7` removes more state than it adds. The toggle no longer depends on sidebar state at all, and that dependency produced r3 F6 and F7. So this is **converging, not being patched**.

What is left is one prediction: the toggle's literal offset copies the menu button's box. It is the source of F2 (the units disagree) and F3 (nothing ties the two). A fix that adds a second computed value — a rem `calc`, a measured offset, a breakpoint — would be the next added prediction. A shape where the toggle takes its position from the menu button rather than copying it would remove the prediction, for example both buttons in one fixed row. That call is the supervisor's.

## R-0 items against `2b2ebaa7`

1–6. No product code for page headers or `PageFrame` changed in these commits (`DriveHome.test` gained a mock and a case only). The e2e `page-frame` cases pass. Unchanged from r3.
7. Route set, hidden-below-md-with-a-file and "no page header draws one" hold (M7–M9, M17). Fixed position independent of sidebar state holds. "Beside the menu button" breaks at root font sizes above 16px (F2) and is held by no test against the menu button (F3). "No app header draws one" holds in code but not in tests (F4). r3 F1 (collection loading/not-found) is unchanged and remains recorded only.
8. Holds (M18).

TOTAL: 4 findings
