# PR #265 review, round 2 — fix `aa6ed1859f7c82364954fe308edb6941cc2142ac`

Parent `f15cecf9` (round 1). Worktree checked out at the SHA (`git rev-parse HEAD` = `aa6ed185…`,
`git status --short` empty before and after). Mutations are written to copies in the scratchpad and
bind-mounted over `/app/app/...` / `/app/tests/...`; the worktree is not edited.

Baseline at `aa6ed185`: `99 passed` for `tests/test_addon_navigation.py tests/test_addons_status_drive.py
tests/test_startup_lifespan.py` (round 1 baseline at `f15cecf9`: 75).

## Q1 — Did the fix do what round 1 asked?

Yes, all three.

**F-1 reproduction** (scratch `tests/test_r265b_repro.py`, reusing `registry`/`manifests` from
`test_addon_navigation.py` and `addons_dir`/`_write_addon`/`_load` from `test_startup_lifespan.py`).
Placements `["primary"]`, `{"x": 1}`, `1`, `None`; manifest path asserts `badnav` stays in
`get_external_addons()` with no `navigation`; in-process path asserts `register_in_process` returns
True with no `navigation`; plus a `_load_addons` test (revised invariant 2) that writes a real addon
package with `on_startup` and an invalid `navigation` (`placement: ["primary"]`, `placement: null`,
`icon: null`) and asserts the addon is in `_loaded_addons`, has no `navigation`, and its `on_startup` is
queued in `_addon_startup_fns`.

```
aa6ed185 (fix):  11 passed, 1 failed   (the failure is the [pre-existing] scope probe, F-2 below)
f15cecf9 (r1):    6 failed, 6 passed   manifest[list], manifest[dict], in_process[list],
                                       in_process[dict], load_addons[placement list] (+ the scope probe)
                  log: "Failed to load addon: zz_test_addon_badnav" (main.py:255) — on_startup lost
```

So at `f15cecf9` the `_load_addons` path really did drop `on_startup` (round 1 inferred this by reading;
now observed), and at `aa6ed185` it does not.

**Round 1 survivors re-run at `aa6ed185`:**

| id | mutation | want | result |
|---|---|---|---|
| M4 | `_VALID_PLACEMENTS` − `sources` | kill | killed (2 failed: valid-placement `sources` × 2 paths) |
| M5 | `_VALID_PLACEMENTS` − `utility` | kill | killed (2 failed) |
| M12 | `key in nav` → `nav.get(key) is not None` | kill | killed (4 failed: `i18n-key-null`/`icon-null` × 2 paths) |

## Q2 — What did the fix break?

Nothing observable. Every mutation of the new guard that changes behaviour is killed; the two survivors
of the guard are equivalent mutants (N3, N4). One survivor against revised invariant 2 is a test gap in
unchanged `main.py` code, reported as F-1.

## F-1 [introduced] Revised invariant 2's "`on_startup` still runs" clause is held only transitively; no test loads an in-process addon with an invalid `navigation` through `_load_addons` (proposed bucket B; supervisor may rate A as "a test that lets one through")

The clause is new (supervisor revision after r1) and the in-process tests call `register_in_process`
directly (`_register` asserts `is True`). That kills every mutation inside `addon_registry` that could lose
`on_startup` (N9: raise for in-process → 22 failed; N10: return False when navigation dropped → 21
failed), because `main._load_addons` queues `on_startup` unconditionally after a non-raising call.

What survives is a mutation in `main._load_addons` that makes the hook depend on the navigation verdict:

```
<             if hasattr(mod, "on_startup"):
>             if hasattr(mod, "on_startup") and not ("navigation" in getattr(mod, "ADDON_META", {}) and "navigation" not in (addon_registry.get(name) or {})):
```

N11: want=kill, result **survived** (`99 passed`). A fixture observes it: the scratch
`test_load_addons_invalid_navigation_keeps_on_startup` fails 3/3 under N11. Not inert, but the mutant
requires `main.py` (untouched by this PR) to read `navigation`, which no plausible edit does; the realistic
break path (an exception out of `register_in_process`) is held. Hence proposed B. If the supervisor wants
the clause held directly, the scratch `_load_addons` test above is the shape (one param suffices).

## F-2 [pre-existing] `_validate_scope` has the same unhashable-`in`-on-a-set shape F-1(r1) had: a list/object `scope` raises out of `register_in_process` (proposed bucket B)

`scope not in _VALID_SCOPES` with `ADDON_META = {"scope": ["drive"]}`:

```python
def test_in_process_unhashable_scope_pre_existing(registry):
    assert addon_registry.register_in_process("badscope", {"label": "x", "scope": ["drive"]}) is False
```

```
aa6ed185:  E  TypeError: unhashable type: 'list'
0ae8c2e7:  E  TypeError: unhashable type: 'list'     (develop's addon_registry.py mounted)
```

Identical on develop, so `[pre-existing]`; not in this PR's scope. Effect: in `_load_addons` the exception
skips the `on_startup` append, whereas a string-invalid scope keeps it; manifest path just logs a traceback
for an addon that is skipped anyway. Only a first-party `ADDON_META` author can reach it. Ledger, not fix.

## Q3 — Trajectory

There is one fix diff (`aa6ed185`), so the "two rounds in a row" pattern cannot yet exist. Its shape:

- **Code:** one condition added to an existing branch (`not isinstance(placement, str) or …`). No new
  branch, state, or return value. It makes `placement` follow the same type-then-value pattern `label` and
  `priority` already used in `f15cecf9`, i.e. it removes the one check that differed rather than adding a
  special case. The `in`-on-a-set risk is now absent from `_navigation_error` (remaining checks are
  `isinstance`, `.strip()` after `isinstance`, and `key in nav` on a proven dict).
- **Tests:** adds predictions — four non-string placements, two nulls, `sources`/`utility` valid cases,
  and one not asked for by r1: `test_navigation_without_optional_keys_is_valid`. That last one closed an
  unreported gap: N8 (`if key in nav and` → `if`, i.e. KeyError when `icon`/`i18n_key` absent) **survived
  at `f15cecf9` (`75 passed`)** and is killed at `aa6ed185` (2 failed).

Answer: this round adds a condition and test predictions, but no earlier fix round exists to repeat, and the
condition converges the validator's shape rather than patching around it. No sign of design patching.

## Mutation table (all at `aa6ed185`, three target test files unless noted)

Diffs of each mutant against the original were printed and checked non-empty before running.

| id | file | mutation | want | result | notes |
|---|---|---|---|---|---|
| M4 | addon_registry | `_VALID_PLACEMENTS` − `sources` | kill | killed (2) | r1 F-2 closed |
| M5 | addon_registry | `_VALID_PLACEMENTS` − `utility` | kill | killed (2) | r1 F-2 closed |
| M12 | addon_registry | `key in nav` → `nav.get(key) is not None` | kill | killed (4) | r1 F-3 closed |
| N1 | addon_registry | delete `not isinstance(placement, str) or` | kill | killed (6) | list/object × 3 tests; manifest traceback logged |
| N2 | addon_registry | guard `or` → `and` | kill | killed (8) | accepts `"views"`, raises on list |
| N3 | addon_registry | `isinstance(placement, str)` → `(str, int, NoneType)` | live | survived | inert: equivalent (int/None hashable, rejected by membership) |
| N4 | addon_registry | set membership → tuple membership | live | survived | inert: equivalent (tuple `in` never raises) |
| N6 | addon_registry | `placement = nav.get("placement") or "primary"` | kill | killed (4) | null and missing |
| N7 | addon_registry | `nav.get("placement", "primary")` | kill | killed (2) | missing |
| N8 | addon_registry | `if key in nav and not isinstance` → `if not isinstance` | kill | killed (2) | new optional-keys test; survived at `f15cecf9` |
| N9 | addon_registry | raise after warning when `type == "in_process"` | kill | killed (22) | revised inv. 2 no-raise |
| N10 | addon_registry | `register_in_process` returns False when navigation dropped | kill | killed (21) | |
| N12 | addon_registry | `placement` present and null → valid | kill | killed (2) | `placement-null` param |
| N11 | main | `on_startup` queued only if navigation not dropped | kill | **survived** | F-1; scratch `_load_addons` test kills it (3 failed) |
| T1 | test | remove `placement-list` and `placement-object` params | live | survived (93) | alone |
| T1+N1 | test+registry | T1 with guard deleted | live | survived | those two params are the sole holders of the guard; `placement-number` cannot hold it |
| T2+N12 | test+registry | remove `placement-null` param, null→valid | live | survived | `placement-null` is the sole holder of revised inv. 3 "null" |
| T3+M4 | test+registry | valid-placement param only `primary`, minus `sources` | live | survived | the new parametrize is the sole holder |
| T4+M12 | test+registry | remove `i18n-key-null`/`icon-null`, null accepted | live | survived | the two null params are the sole holders |

Invariants as revised: 1 holds (all three placements, both paths); 2 holds on both paths, including
`on_startup` via `_load_addons` (observed with the scratch test), held by the suite only transitively (F-1);
3 holds for number/array/object/null placement and null `i18n_key`/`icon`, no raise; 4, 5, 6, 7 untouched
by the fix diff (no change to `main.py`, `_without_invalid_navigation`, or call sites) and their r1 killers
still pass in the 99.

TOTAL: 2 findings
